---
name: INDEX
description: Master index of documentation and design artifacts in 4thBrain v04 Java/Spring Boot
metadata:
  version: 2.4
  created-by: Claude Code
  date: 2026-09-07
---

# INDEX — 4thBrain v04 Documentation & Artifacts

Catalog of project documentation, design documents and build configuration.

Source files under `src/` are not indexed. The compiler, the IDE and `git log` already track
them, and duplicating that here only produced rows that drifted from the code.

| File Name | History |
|-----------|----------|
| CLAUDE.md | [2026-09-03] Project overview, three-phase delivery plan (Skeleton → Real Logic → Testing), repository structure, design decisions inherited from v03<br>[2026-09-07] Repository structure corrected — `chat.html`, `styles.css` and `client.js` were listed but have never existed, and `templates/` was absent. Added a design principle covering how to add a UI page under ADR25, including the `${...}` template-literal trap<br>[2026-09-07] Recorded the governance trim: the merge-to-v03 and md-memory rules and both batch skills removed, along with the leftover `documets/` directory |
| PROJECT_4thBrain.md | [2026-09-03] Phase 1–3 deliverables and stories, key design decisions (Actuators, Coordinator, Monitor, database), mapping to v03 requirements (FR1–FR9, NFR1–NFR12)<br>[2026-09-06] Added Story P1.8 (Document Copies): remove `path` from document table/entity, add `document_copy` table keyed by vault area with created_at/end_date, add `source_url` to document; design decided, ready to implement<br>[2026-09-06] Story P1.8 implemented; added Story P1.9 (Actuator Instantiation & Registration): single owner for actuator creation, registration and start moved out of the constructor, Coordinator routing map keyed by class simple name<br>[2026-09-06] Added Stories P1.10 (actuator run loop), P1.11 (Coordinator entry point and message addressing), P1.12 (status endpoint reports document counts), P1.13 (text/URL ingestion endpoints and the stale controller tests)<br>[2026-09-06] P1.9 story updated to reference the detailed design in documents/design/STARTUP-SEQUENCE.md; added configuration notes about clipper and briefing thread counts<br>[2026-09-07] Added Story P2.8 (MarkItDown spike): evaluate microsoft/markitdown against Apache Tika as the Extractor's converter, and settle how Spring Boot calls an external Python process; P2.3 blocked on it because its stack is v03's JavaScript one<br>[2026-09-07] Added Story P3.5 (Upgrade to JUnit 5); renamed documets/ to documents/; created individual story files under documents/story/<br>[2026-09-07] Added Story P1.14 (View Layer & Template Engine) to Phase 1, closing BUG-001; version 1.3<br>[2026-09-07] P1.9 and P1.10 recorded as verified together, with the three defects found in doing so; Status line changed from "Design Phase" to "Phase 1 in progress — the application boots as of P1.9"; added Stories P1.15 (Orderly Shutdown) and P1.16 (Crash Recovery), renumbered from the P1.14/P1.15 they were drafted as; version 1.4<br>[2026-09-07] Added Story P1.17 (Spike: A Unified Composer) to Phase 1; version 1.5 |
| BACKLOG-TRACKER.md | [2026-09-06] Delivery status of all Stories grouped WIP / READY / NOT-READY / COMPLETED, plus a Summary table covering Stories and Bugs; seeded from PROJECT_4thBrain.md<br>[2026-09-06] Summary split into four tables, one per status, each carrying the blocker or completion date; the COMPLETED section's duplicate table dropped<br>[2026-09-07] P2.8 added to READY, P2.3 gains it as a second blocker, counts updated to 25 stories<br>[2026-09-07] Restructured: detailed story descriptions moved to individual files under documents/story/, BACKLOG-TRACKER references them; P3.5 added to READY; counts now 26 stories<br>[2026-09-07] Bugs section added carrying BUG-001; the stale "No Bugs are currently logged" line replaced; BUG-001's blocker changed from P1.7 to the undecided choice of fix<br>[2026-09-07] P1.14 added to READY and BUG-001 moved there once its fix was decided; counts now 27 stories<br>[2026-09-07] P1.10 moved to COMPLETED alongside P1.9, P1.11 unblocked into READY, P2.1–P2.7's blocker restated as P1.11, P1.15 and P1.16 added to READY; counts now 29 stories<br>[2026-09-07] P1.17 (Spike: A Unified Composer) added to READY; counts now 30 stories, version 2.0 |
| build.gradle | [2026-09-03] Gradle build config, Spring Boot 3.2.0, JPA, SQLite JDBC 3.44.0.0, Hibernate SQLite dialect<br>[2026-09-06] `sourceCompatibility` moved inside a `java { }` block, which Gradle 9 requires; Gradle wrapper added<br>[2026-09-07] Added `spring-boot-starter-thymeleaf` for P1.14 — without a view technology every `@Controller` view name resolved to nothing<br>[2026-09-07] `sourceCompatibility` 17 → 21: the code uses `List.getFirst()`, a Java 21 API, and only compiled at 17 because Gradle sets `-source`/`-target` without `--release`<br>[2026-09-07] P3.5: `junit:junit:4.13.2` removed. It sat on the test classpath with no `junit-vintage-engine`, so a JUnit 4 test compiled and then reported zero tests instead of failing. Jupiter comes transitively from `spring-boot-starter-test`, so nothing replaces it |
| documents/design/SYSTEM-REQUIREMENTS-SPECIFICATION.md | [2026-09-03] Functional Requirements FR1–FR9, Non-Functional Requirements NFR1–NFR12 (reference to v03, no changes)<br>[2026-09-07] No file exists at this path in v04 — the specification lives in v03 and is referenced, not copied. Row kept as the pointer to it |
| src/main/resources/schema.sql | [2026-09-03] SQLite schema, inherited from v03<br>[2026-09-07] Path corrected. The row previously read `documents/design/schema.sql`, where no file has ever existed; the live schema Spring runs at startup is this one, under resources. Indexed by exception to the `src/` rule because it is a schema definition rather than source |
| documents/design/ADRS.md | [2026-09-03] Architecture Decision Records (reference to v03: ADR14, ADR17, ADR24)<br>[2026-09-07] File actually created — it had been indexed and cited since 2026-09-03 but never written. Records that ADR1–ADR24 stay inherited from v03, and adds ADR25: Thymeleaf is the view layer, served pages live in `templates/`, `static/` holds only linked assets. Written up with the counterargument (it buys nothing today) and the `${...}` JavaScript-vs-Thymeleaf trap<br>[2026-09-07] ADR28 added — MarkItDown invoked as a subprocess is the Extractor's Markdown converter, closing Story P2.8's decision criterion. Decided on the argument rather than on the corpus measurements, which have not been run, and says so: the case is that a hard timeout only exists across a process boundary, and this pipeline makes a stalled document invisible because it is driven by `Document.status`. Records that on PDF both candidates are flat text extractors, so "built for LLM consumption" cannot carry the argument; carries three falsifiable triggers, two of which reverse the decision. Numbered 28 because ADR26 is reserved by P1.11's plan and ADR27 by P1.17's spike |
| documents/design/SPIKE-MARKITDOWN.md | [2026-09-07] Story P2.8 brief: what MarkItDown is (MIT, Python ≥ 3.10, CLI / library / MCP server, formats and their underlying parsers), what it would and would not replace in the pipeline, six options for calling an external Python process from Spring Boot compared side by side, Apache Tika as the no-Python comparison arm, what the spike must measure, exit criteria, risks, one-day timebox |
| documents/design/SPIKE-UNIFIED-COMPOSER.md | [2026-09-07] Story P1.17 brief: collapse the Add File, Add Text, Add URL and Chat panels into one composer; five options for telling capture from conversation in a single input (mode toggle, two send buttons, sigil, inferred intent — rejected, and ingest-only unification as the fallback); a narrow URL detection rule firing only when the whole input is one URL; attachment, tagging and receipt-feed questions; the three endpoints still stubbed behind the screen; ADR25 constraints; exit criteria and a half-day timebox |
| documents/design/STARTUP-SEQUENCE.md | [2026-09-06] Story P1.9 design: bean initialization order, ActuatorManager, three phases (create+inject+name, register all, start all), why the current sequence aborts, sequence diagram<br>[2026-09-06] Rewritten as built rather than proposed: registry documented as ConcurrentHashMap of CopyOnWriteArrayList with `instancesOf`, `assignName` explained, shutdown section updated for the working `shutdown()` and the interrupt break, configuration section states what is in application.yaml, and the closing note on the removed `Actuator.map`/`registry` fields folded into the P1.10 section |
| documents/P1.9-FIX-PLAN.md | [2026-09-06] Execution plan for P1.9: ActuatorManager/Coordinator code already implemented but unverified; three defects found (unsynchronized registry read, null next-actuator dereference, spurious-interrupt hot spin); new ActuatorManagerTest; boot verification steps against acceptance criteria<br>[2026-09-06] Plan executed in full; all six P1.9 acceptance criteria verified |
| scripts/build-log.ps1 | [2026-09-06] Wrapper for gradlew tasks: tees output to @logs/v04-\<name\>.log as it streams (survives interruption) and writes a keyed, replaced exit code per task to @logs/exit-codes.txt. Replaces the prose redirect-by-hand convention in clear-edits.md, which had gone stale (last log 10 hours old, nothing enforcing it)<br>[2026-09-06] Added an optional `-Extra` string array passed through to gradlew, so scoped runs such as `-Task test -Extra @('--tests','*ActuatorManagerTest')` still go through the wrapper |
| documents/P1.11-FIX-PLAN.md | [2026-09-07] Execution plan for P1.11: story claims verified line by line against the tree, two corrections (the Message NPE is latent, and `sendMessage` has a second unguarded dereference) and two further defects found (`Message.builder()` never stamps `createdAt`; `id()` misspells "Mesasge"). Design gap flagged — ADR26 required before code, because clearing a static registry on context close does not give test isolation under Spring's context cache. Change specified across Coordinator, the four registry call sites, IngestionController and Message; new CoordinatorTest; IngestionControllerTest's P1.13 methods to be `@Disabled` rather than left red |
| documents/P3.5-UPGRADE-PLAN.md | [2026-09-07] Execution plan for P3.5: the JUnit 5 migration had already happened before the story was written — both test classes are Jupiter, no `@RunWith`/`@Rule`/`org.junit` import exists. What remains is deleting `junit:junit:4.13.2` from build.gradle, which sits on the test classpath with no vintage engine, so a JUnit 4 test compiles and is then silently not executed. Adjacent `@MockBean` → `@MockitoBean` finding recommended for the same pass; verification is a baseline-and-compare on executed test count |
| scripts/batch-p2.8-submit.ps1 | [2026-09-07] Submits the P2.8 research to the Anthropic Message Batches API as three self-contained requests (analysis with the web_search server tool, ADR-shaped conclusion, implementation story), each carrying SPIKE-MARKITDOWN.md, P2.8, P2.3 and a repository-facts block. Writes the batch id to @batch/ |
| scripts/batch-fetch.ps1 | [2026-09-07] Checks a message batch and, once ended, writes each result to @batch/\<custom_id\>.md keyed by custom_id rather than position. Does not poll |
| documents/story/P1.8.md | [2026-09-07] Remove document.path, add document_copy table and source_url field; vault areas (tmp/incoming/indexing/raw) |
| documents/story/P1.9.md | [2026-09-07] Actuator instantiation and registration — single owner (ActuatorManager), ordered lifecycle, fixing double-instantiation NPE<br>[2026-09-07] Status section added: implemented and verified 2026-09-06 by boot log and `ActuatorManagerTest`, with the three defects found while verifying. Status COMPLETED |
| documents/story/P1.10.md | [2026-09-07] Actuator run loop — blocking queue, ordered shutdown, bounded performance, fix busy-spin and core usage<br>[2026-09-07] Status section added: landed in the same pass as P1.9 and verified with it; idle actuator threads use no measurable CPU. Status COMPLETED |
| documents/story/P1.11.md | [2026-09-07] Coordinator entry point and message addressing — settle on startChain(), fix message logging NPEs |
| documents/story/P1.12.md | [2026-09-07] Status endpoint reports document counts from database instead of actuator queue depths |
| documents/story/P1.13.md | [2026-09-07] Text and URL ingestion endpoints, rewrite 23 controller tests against current contract |
| documents/story/P1.14.md | [2026-09-07] View layer and template engine — add Thymeleaf, move index.html into templates/, replace the System.out stubs in both controllers; closes BUG-001, design in ADR25, verification waits on P1.9<br>[2026-09-07] Implemented and verified against a running application: all six endpoints return 200, the inline script's `${...}` literals survive rendering, `static/` is gone. The P1.9 dependency proved already satisfied. Status COMPLETED |
| documents/story/P1.15.md | [2026-09-07] Orderly shutdown — `shutdown()` on every actuator, bounded join with a log line for a thread that overruns it, and an endpoint to stop the application cleanly. Drafted as P1.14 on the P1.9 verification branch; renumbered on rebase |
| documents/story/P1.16.md | [2026-09-07] Crash recovery — requeue documents left in a transient status on startup, with an age threshold and a vault check so live or already-indexed work is not re-processed. Drafted as P1.15; renumbered on rebase |
| documents/story/P1.17.md | [2026-09-07] Spike: a unified composer — one screen with a text area, a `[+]` attach button and automatic URL detection, replacing the three ingest panels and the chat panel. Names the routing question (capture versus converse) as the thing the spike exists to settle, and sequences it before P1.13 because the composer constrains what those endpoints accept. Its ADR is numbered 27, ADR26 being claimed by P1.11's plan |
| documents/story/P2.1.md | [2026-09-07] OllamaClient and ConcurrencyGate for bounded LLM concurrency (Semaphore(1)) |
| documents/story/P2.2.md | [2026-09-07] Ingestor real logic — read files from RAW_DIR, create Document records, route to TextExtractor |
| documents/story/P2.3.md | [2026-09-07] TextExtractor real logic — document conversion to Markdown; extraction stack TBD by P2.8 spike<br>[2026-09-07] Rewritten against ADR28. The v03 Node stack (OpenDataLoader, Turndown, Mammoth) is struck; the story now consumes the `MarkdownConverter` seam, archives the original before conversion, and carries P2.9's failure-reason-to-status mapping as a binding contract. Blocker changed from P2.8 (a decision) to P2.9 (a component). Records that no acceptance criterion may assume Markdown headings come back from a PDF. Version 2.0 |
| documents/story/P2.4.md | [2026-09-07] Classifier real logic — LLM tagging via OllamaClient, create Classification and DocumentTag records |
| documents/story/P2.5.md | [2026-09-07] Indexer real logic — vault storage and Smart Connections MCP indexing |
| documents/story/P2.6.md | [2026-09-07] Briefing real logic — scheduled synthesis of recent documents via OllamaClient |
| documents/story/P2.7.md | [2026-09-07] File watcher — monitor RAW_DIR for automatic ingestion via WatchService or polling |
| documents/story/P2.8.md | [2026-09-07] Spike: MarkItDown vs Apache Tika as document converter; six integration options analysis; results in ADR and P2.3 rewrite<br>[2026-09-07] Status section added. Three of five acceptance criteria met — analysis, ADR28, P2.3 rewritten — and two not: no fixture corpus and no measurements of any kind. Stays WIP rather than being closed on a decision taken without them. Records the format census and the scanned-PDF split as the two commands to run next, either of which can reverse the stack |
| documents/story/P2.9.md | [2026-09-07] MarkdownConverter seam and its MarkItDown subprocess implementation, carrying out ADR28: the three-method interface, the `com.fourthbrain.convert` package, the full conversion path including staging, stream redirection and descendant-killing on timeout, a startup probe that converts a real non-ASCII fixture rather than calling `--version`, the failure-reason-to-status mapping, 18 acceptance criteria, and eight design placeholders still waiting on P2.8's measurements |
| documents/story/P3.1.md | [2026-09-07] Unit tests for actuators (JUnit 5, Mockito, >80% coverage of process() logic) |
| documents/story/P3.2.md | [2026-09-07] Integration tests for full message flows through actuator chain (multiple stages, concurrency) |
| documents/story/P3.3.md | [2026-09-07] REST API tests via MockMvc for all endpoints (file/text/URL ingestion, status, search, chat) |
| documents/story/P3.4.md | [2026-09-07] Manual smoke test — browser UI, file upload, watch pipeline progress through dashboard |
| documents/story/P3.5.md | [2026-09-07] Upgrade to JUnit 5 (Jupiter) — migrate all tests from JUnit 4, replace annotations and lifecycle<br>[2026-09-07] Rewritten against what the tree actually contained: the migration had already happened, and the story's real subject is a dangling `junit:junit` on the test classpath with no vintage engine, which turns a JUnit 4 test into a silent zero-test pass. Status section added recording the removal, the `@MockBean` → `@MockitoBean` change folded in, and the four verifications. Status COMPLETED |
| documents/bug/BUG-001.md | [2026-09-07] Web UI fails to load on localhost:8080 or localhost:8080/chat; UIController routing or static resources issue; blocks P1.13 and P3.4<br>[2026-09-07] Diagnosed against the code: UIController and AdminController are `@Controller`s returning view names, and build.gradle carries no template engine, so the names resolve to nothing; `@GetMapping("/")` also suppresses the welcome page that would otherwise serve static/index.html. Missing CSS/JS and static-resource config ruled out. Three fix options recorded, none chosen<br>[2026-09-07] Fix decided: Thymeleaf (ADR25), carried out by Story P1.14. Recorded that the cheaper option was the better one on the evidence and was passed over for roadmap reasons, so the tradeoff survives review. Status READY<br>[2026-09-07] Closed by P1.14 and verified against a running application |
| documents/bug/BUG-002.md | [2026-09-07] A fresh clone will not start: `application.yaml` points SQLite at `data/fourthbrain.db`, SQLite creates the file but not its parent directory, and `data/` is untracked. Found during P1.14 verification, where it masked P1.9's true status. Three fix options; `vault/` and `tmp/` have the same gap |
