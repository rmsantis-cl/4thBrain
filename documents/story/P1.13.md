---
name: P1.13
description: Text and URL ingestion endpoints with working tests
metadata:
  version: 1.0
  created-by: Claude Haiku 4.5
  date: 2026-09-07
  story-id: P1.13
  status: NOT-READY
  depends-on: P1.11
  blocks: P3.3
---

# Story P1.13 — Text and URL Ingestion Endpoints

## Problem

Two of the three ingestion endpoints do nothing, and their tests assert a design that was removed.

`IngestionController.submitText` and `submitUrl` log `Not implemented` and return `{"not":"implemented"}`. The real body of `submitText` is present but commented out, and it calls `databaseService.createDocument("text", text)` — passing a literal as what was then the path column, which P1.8 has since removed.

All 23 tests in `IngestionControllerTest` fail. They assert `$.jobId`, mock `databaseService.createDocument(...)`, and verify `coordinator.startChain(1L)` — the Job-based design that was deliberately removed.

## Change

1. Implement `submitText`: create a Document from the posted text with `content` set and no copy row, since nothing is on disk, then start the chain
2. Implement `submitUrl`: create a Document with `source_url` set, then start the chain so `Clipper` fetches it
3. Rewrite the tests against the current contract: `id` rather than `jobId`, proper service calls, and the working entry point from P1.11
4. Decide the response shape once and apply it to all three endpoints

## Acceptance Criteria

- `POST /api/ingest/text` and `/url` create a Document, start the pipeline, and return the agreed shape
- A URL submission produces a document with `source_url` set and no `document_copy` row
- `gradle test` passes, with no test asserting `jobId` or any other part of the Job design