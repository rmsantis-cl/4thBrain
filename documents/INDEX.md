<<<<<<< ours
---
name: INDEX
description: Index of design documents, execution plans, stories and bugs under documents/ in 4thBrain v04
metadata:
  version: 2.12
  created-by: Claude Sonnet 5
  date: 2026-09-08
---

# INDEX — 4thBrain v04 documents

Catalog of everything under `documents/`, subdirectories included. Paths are relative to this
file. The repository root and `scripts/` carry their own INDEX; see
[../INDEX.md](../INDEX.md).

| File Name | History |
|-----------|----------|
| design/SYSTEM-REQUIREMENTS-SPECIFICATION.md | [2026-09-03] Functional Requirements FR1–FR9, Non-Functional Requirements NFR1–NFR12 (reference to v03, no changes)<br>[2026-09-07] No file exists at this path in v04 — the specification lives in v03 and is referenced, not copied. Row kept as the pointer to it |
| design/ADRS.md | [2026-09-03] Architecture Decision Records (reference to v03: ADR14, ADR17, ADR24)<br>[2026-09-07] File actually created — it had been indexed and cited since 2026-09-03 but never written. Records that ADR1–ADR24 stay inherited from v03, and adds ADR25: Thymeleaf is the view layer, served pages live in `templates/`, `static/` holds only linked assets. Written up with the counterargument (it buys nothing today) and the `${...}` JavaScript-vs-Thymeleaf trap<br>[2026-09-07] ADR28 added — MarkItDown invoked as a subprocess is the Extractor's Markdown converter, closing Story P2.8's decision criterion. Decided on the argument rather than on the corpus measurements, which have not been run, and says so: the case is that a hard timeout only exists across a process boundary, and this pipeline makes a stalled document invisible because it is driven by `Document.status`. Records that on PDF both candidates are flat text extractors, so "built for LLM consumption" cannot carry the argument; carries three falsifiable triggers, two of which reverse the decision. Numbered 28 because ADR26 is reserved by P1.11's plan and ADR27 by P1.17's spike<br>[2026-09-07] ADR26 written, closing the reservation: `startChain` is the sole entry point, a message carries the fully-loaded `Document` rather than an id (so no actuator re-fetches one it already holds), `Message.from` is null-safe, the Coordinator registry becomes instance state, a submitted URL lives in `source_url`, `DatabaseService` alone writes `Document.status`, and `/api/ingest/text`/`/url` collapse into one `POST /api/ingest/capture` dispatching on which body key is present — extending the decision from P1.11 into P1.13. ADR28's footnote corrected to show only ADR27 as still unwritten<br>[2026-09-07] ADR27 written, closing the last reservation: one composer shaped like a chat box, `[+]` `ASK` `URL` `SEND` inside the container, the user naming the action and detection only enabling the `URL` button — which lights only when the whole trimmed input is a single `http`/`https` URL, so `SEND` on a bare URL storing it as text is the override. Text plus an attached file is two submissions and two Documents; several files are one Document each; no tags field; Chat folds in, so the nav drops from six items to four. Records that it was taken without the prototype Story P1.17 called for, and what would reopen it. ADR28's footnote updated again — both reservations are now written |
| design/SPIKE-MARKITDOWN.md | [2026-09-07] Story P2.8 brief: what MarkItDown is (MIT, Python ≥ 3.10, CLI / library / MCP server, formats and their underlying parsers), what it would and would not replace in the pipeline, six options for calling an external Python process from Spring Boot compared side by side, Apache Tika as the no-Python comparison arm, what the spike must measure, exit criteria, risks, one-day timebox |
| design/SPIKE-UNIFIED-COMPOSER.md | [2026-09-07] Story P1.17 brief: collapse the Add File, Add Text, Add URL and Chat panels into one composer; five options for telling capture from conversation in a single input (mode toggle, two send buttons, sigil, inferred intent — rejected, and ingest-only unification as the fallback); a narrow URL detection rule firing only when the whole input is one URL; attachment, tagging and receipt-feed questions; the three endpoints still stubbed behind the screen; ADR25 constraints; exit criteria and a half-day timebox |
| design/STARTUP-SEQUENCE.md | [2026-09-06] Story P1.9 design: bean initialization order, ActuatorManager, three phases (create+inject+name, register all, start all), why the current sequence aborts, sequence diagram<br>[2026-09-06] Rewritten as built rather than proposed: registry documented as ConcurrentHashMap of CopyOnWriteArrayList with `instancesOf`, `assignName` explained, shutdown section updated for the working `shutdown()` and the interrupt break, configuration section states what is in application.yaml, and the closing note on the removed `Actuator.map`/`registry` fields folded into the P1.10 section |
| P1.11-FIX-PLAN.md | [2026-09-07] Execution plan for P1.11: story claims verified line by line, two corrections (the Message NPE is latent, and `sendMessage` has a second unguarded dereference) and two further defects found (`Message.builder()` never stamps `createdAt`; `id()` misspells "Mesasge"). Design gap flagged — an ADR required before code, because clearing a static registry on context close does not give test isolation under Spring's context cache<br>[2026-09-07] `UI-ENTRY-POINTS-PLAN.md` merged in and deleted, so one plan covers the run rather than two covering the same stories. Now four staged: P1.11 (entry point), P1.17 (composer spike, in parallel), P1.13 (text and URL endpoints), P1.18 (the composer itself, story still to be created). New finding carried in: P1.13 cannot meet its own acceptance criteria as written, because `Ingestor.isUrl` and `Clipper` both read a submitted URL from `content` rather than `source_url`, so a document built as the story describes stops at the first hop with no error. Correction to the merged plan's premise: it claimed `DocumentService.getDocumentById` and `setStatus` do not exist — both do, at `DocumentService.java:40` and `:105`. The sketch still uses `DatabaseService`, because that is the synchronized service under ADR17 and the one `IngestionController` injects, which surfaces a real inconsistency the plan now logs — two services write `Document.status` and only one is synchronized. Filename kept though it is narrower than the contents; version 2.0<br>[2026-09-07] Stage 3 rewritten against ADR26 decision 6: `/text` and `/url` collapse into one `POST /api/ingest/capture`, dispatching on whichever of a `url` or `text` body key is present, rather than staying two endpoints. Design-decisions list gained this as item 6, noting ADR26 has since been written. Acceptance-criteria mapping row for P1.13 split to add "`/text` and `/url` no longer exist". Version 2.1 |
| plan-11-13-17.md | [2026-09-07] Row added — the file existed and was referenced from `BACKLOG-TRACKER.md` but had no INDEX entry. Alternative plan delivering P1.11, P1.13 and P1.17 as one change rather than four staged deliveries; not yet chosen over `P1.11-FIX-PLAN.md`<br>[2026-09-07] Step 5, step 6 and the acceptance-criteria mapping updated to match ADR26 decision 6: `/text` and `/url` collapse into one `POST /api/ingest/capture`. Notes that ADR26 (its own step 0 draft) has since actually been written; only ADR27 remains open. Version 1.1 |
| plan-P1.18.md | [2026-09-07] Execution plan for P1.18 (The Unified Composer). Owns `index.html` alone and touches no Java, so it merges independently of the other four plans in the set. No new ADR — ADR27 settles the design. Seven steps: nav from seven items to four, the composer container and its CSS, ADR27's URL rule implemented as stated, four submit handlers, an escaping feed, deletion of the four superseded panels and their JavaScript, and ADR25's constraints restated. Carries a found defect: the chat panel interpolates user text into `innerHTML` unescaped, and the rewrite must not copy that into the feed. Ten manual verification steps including ADR27's URL truth table; DD-2 and DD-5 recorded as carried rather than closed<br>[2026-09-07] Version 1.1 — `review-plan-p1.18.md` folded in, with a disposition table recording six findings taken and one rejected. Three were real defects in v1.0: the plan said files upload on attach and also that they stage, which contradict; re-enabling the buttons in a `finally` would have lit `URL` against ADR27's rule, so all state now goes through one `updateComposerState()`; and blanket-clearing after a send destroyed the user's note when one of several uploads failed, so nothing is cleared until its own request returns 2xx. Rejected: the claim that `/api/ingest/file` omits `status` — it returns it at `IngestionController.java:139`, and the review quotes a four-key map that does not match the file. Also gained an `ASK` prompt bubble, labelled receipts, `white-space: pre-wrap`, a drag counter, textarea auto-grow, and an optional `Ctrl+Enter` accelerator marked as going past ADR27. Verification grew from ten steps to fifteen |
| review-plan-p1.18.md | [2026-09-07] Technical review of `plan-P1.18.md` against the codebase: three state and logic contradictions, one claimed contract mismatch, and several interaction traps, with drop-in CSS and JavaScript. Folded into `plan-P1.18.md` v1.1, where its disposition table records six findings taken and finding 1.4 rejected — `/api/ingest/file` does return `status` |
| plan-P2.1.md | [2026-09-07] Execution plan for P2.1 (OllamaClient & ConcurrencyGate). Creates a new `com.fourthbrain.llm` package no other plan opens, and publishes the `OllamaClient` interface `plan-P2.4.md` codes against byte-identically. No new ADR. Departs from the story in one place: it does not inject into `Classifier` (that file is P2.4's) and `Briefing` does not exist, so that criterion re-maps. Records that `ollama.url` already ends in `/v1`, that the gate needs a `tryAcquire` timeout or a hung model parks an actuator thread invisibly, and that P2.10's status is disputed between the tracker (COMPLETED) and its own story header (READY) |
| plan-P2.2.md | [2026-09-07] Execution plan for P2.2 (Ingestor Real Logic), gated on **ADR29**. Two questions the ADR must answer: whether the Ingestor reads `$RAW_DIR` (recommended no — ADR26 makes `startChain` the only entry point and P2.7 owns directory watching), and what it does instead. Three findings drive the answer: a captured text document has no file and no copy row, so a typed note reaches the Indexer and stops there; nothing writes to the `raw` area though ADR28's failure semantics assume the original survives; and the routing table sends text past the Classifier, which is registered, threaded and unreachable. Steps materialise content to disk, archive the original, validate, and route from the ADR's table |
| plan-P2.4.md | [2026-09-07] Execution plan for P2.4 (Classifier Real Logic), gated on **ADR31**, which closes DD-3. Four decisions: the Classifier produces tags (manual tagging stays possible and unbuilt); no `classification` table — `document.topic` plus `document_tag` rows, because `schema.sql` is shared with three parallel branches; the parser extracts the first balanced `{...}` so prose- and fence-wrapped replies work; and a parse failure is not a pipeline failure, which is deliberately the opposite of ADR28's choice for conversion failures. Reproduces P2.1's `OllamaClient` interface verbatim so the branch compiles if it runs first |
| plan-P2.5.md | [2026-09-07] Execution plan for P2.5 (Indexer Real Logic), in two parts. Part A is unblocked and fixes two defects: two documents with the same file name silently overwrite each other in the indexing area, and a captured text document never reaches the vault at all. It writes atomically through a `.part` rename without touching the frozen `DatabaseService`. Part B is blocked on **ADR30**, the "how does Java talk to MCP" question the story itself flagged and ADR28 explicitly declined to answer; the plan recommends running a ten-minute experiment first, on the theory that Smart Connections indexes on the file write and there is no call to make. Records that `vault.path` points inside the repository rather than at the Obsidian vault |
| DESIGN-DEBT.md | [2026-09-07] Row added — the file existed since the P1.11/P1.13 implementation but was never indexed. Open design gaps found mid-plan and logged rather than resolved in the same pass, per `.claude/rules/design-before-implementation.md`. Opened with DD-1: two services write `Document.status` and only `DatabaseService` is synchronized<br>[2026-09-07] DD-2 and DD-3 added from ADR27. DD-2: no endpoint reports the status of one document, so the composer's receipt shows what the server returned at submission and never updates; P1.12 was left alone rather than widened from the outside. DD-3: tags have no owner once the ingest panels are gone, and whether they are typed or produced by the Classifier belongs with P2.4's design<br>[2026-09-07] DD-4 and DD-5 added while planning the five parallel stories. DD-4: `Actuator.run()` stamps the participle unconditionally after `doTheThing` returns, so an actuator has no way to record a failure and a document that failed reads exactly like one that succeeded — which contradicts ADR28's own failure semantics and is tangled with DD-1 in the same three lines. DD-5: `/api/chat/llama` is still the Phase 1 echo stub and no story owns wiring it, so ADR27's `ASK` button returns the stub even after both P1.18 and P2.1 land. Notes added to DD-1 (all five plans freeze `Actuator.java`, so none can close it) and DD-3 (`plan-P2.4.md` is written to close it via ADR31)<br>[2026-09-08] Notes added to DD-2 and DD-4 from Story P1.20. DD-2 records that P1.20 is written to close it and that designing it surfaced a harder constraint than the entry assumed — with no transition history anywhere, a polled endpoint cannot show the states a document went through, only where it is now. DD-4 records that a live status strip raises its stakes, turning a failed stage's wrongly-stamped participle from an unread column value into a green tick on screen<br>[2026-09-08] **DD-3 marked CLEARED** by ADR31, written as Story P2.4's design gate and merged with it: the Classifier produces tags, manual tagging stays possible and unbuilt, and the Markdown-heading trap does not fire because tags arrive as JSON array elements rather than inline `#tokens` — recorded so a later inline-tagging story knows it inherits the trap intact |
| done/P1.9-FIX-PLAN.md | [2026-09-06] Execution plan for P1.9: ActuatorManager/Coordinator code already implemented but unverified; three defects found (unsynchronized registry read, null next-actuator dereference, spurious-interrupt hot spin); new ActuatorManagerTest; boot verification steps against acceptance criteria<br>[2026-09-06] Plan executed in full; all six P1.9 acceptance criteria verified<br>[2026-09-07] Filed under `done/` once the plan completed; the row had been indexed at `documents/P1.9-FIX-PLAN.md`, where the file no longer sits |
| done/P3.5-UPGRADE-PLAN.md | [2026-09-07] Execution plan for P3.5: the JUnit 5 migration had already happened before the story was written — both test classes are Jupiter, no `@RunWith`/`@Rule`/`org.junit` import exists. What remains is deleting `junit:junit:4.13.2` from build.gradle, which sits on the test classpath with no vintage engine, so a JUnit 4 test compiles and is then silently not executed. Adjacent `@MockBean` → `@MockitoBean` finding recommended for the same pass; verification is a baseline-and-compare on executed test count<br>[2026-09-07] Filed under `done/` once the plan completed; the row had been indexed at `documents/P3.5-UPGRADE-PLAN.md`, where the file no longer sits |
| story/P1.8.md | [2026-09-07] Remove document.path, add document_copy table and source_url field; vault areas (tmp/incoming/indexing/raw) |
| story/P1.9.md | [2026-09-07] Actuator instantiation and registration — single owner (ActuatorManager), ordered lifecycle, fixing double-instantiation NPE<br>[2026-09-07] Status section added: implemented and verified 2026-09-06 by boot log and `ActuatorManagerTest`, with the three defects found while verifying. Status COMPLETED |
| story/P1.10.md | [2026-09-07] Actuator run loop — blocking queue, ordered shutdown, bounded performance, fix busy-spin and core usage<br>[2026-09-07] Status section added: landed in the same pass as P1.9 and verified with it; idle actuator threads use no measurable CPU. Status COMPLETED |
| story/P1.11.md | [2026-09-07] Coordinator entry point and message addressing — settle on startChain(), fix message logging NPEs<br>[2026-09-07] Change section corrected to state explicitly that `startChain` loads the document once and passes it by reference through `Message`, matching ADR26 and the run loop's existing behaviour — no per-hop database re-fetch. Status moved to READY now that ADR26 is written. Version 1.1<br>[2026-09-07] Implemented via `plan-11-13-17.md` and verified (`gradle build`/`gradle test` green, new `CoordinatorTest`). Status COMPLETED. Version 1.2 |
| story/P1.12.md | [2026-09-07] Status endpoint reports document counts from database instead of actuator queue depths |
| story/P1.13.md | [2026-09-07] Text and URL ingestion endpoints, rewrite 23 controller tests against current contract<br>[2026-09-07] Rewritten against ADR26 decision 6: `/text` and `/url` collapse into one `POST /api/ingest/capture`, dispatching on whichever body key (`url` or `text`) is present rather than the request path; the UI's own type-detection picks the key. `/file` unchanged. Now depends on P1.17 as well as P1.11, and blocks P1.18. Version 1.1<br>[2026-09-07] Implemented via `plan-11-13-17.md` ahead of P1.17's spike actually running, on the plan's argument that only ADR26's contract-bearing answer (one endpoint) gated this story, not the routing-UI decision. Verified (`gradle build`/`gradle test` green, `IngestionControllerTest` rewritten). Status COMPLETED. Version 1.2 |
| story/P1.18.md | [2026-09-07] Created. Follow-on to P1.17's spike: builds the composer the spike names, wired to P1.13's single `/api/ingest/capture` endpoint. Blocked by P1.13 and P1.17; cannot start until ADR27 is written<br>[2026-09-07] READY — both blockers cleared. Rewritten against ADR27 rather than hedged on the spike's outcome: Chat folds into the composer (four panels removed, not three), the URL rule is stated as the button-enable condition with `SEND` as its override, tags are dropped (DD-3), and per-document status is explicitly not built (DD-2). Acceptance criteria gained the `ASK` path and the four URL-detection cases. Records that the design arrives without prototype backing, so this story's first run is the evaluation the spike skipped<br>[2026-09-07] COMPLETED, with a Status section at the top. Implemented from `plan-P1.18.md` v1.1 in one file (`index.html`, +376/−189) and verified against a running app on port 8081, a pre-existing instance holding 8080. Records what was checked — the rendered page, ADR25's two constraints, all four endpoint contracts including the 400 path, and the database state behind a URL capture versus a text capture — and what stays open: ADR27's reopening question needs a person using the screen, not another pass over the code. Two gaps surfaced and assigned elsewhere: a captured text document has no file and no copy row so it never reaches the vault, and `Clipper` leaves its child document at status `New` |
| story/P1.14.md | [2026-09-07] View layer and template engine — add Thymeleaf, move index.html into templates/, replace the System.out stubs in both controllers; closes BUG-001, design in ADR25, verification waits on P1.9<br>[2026-09-07] Implemented and verified against a running application: all six endpoints return 200, the inline script's `${...}` literals survive rendering, `static/` is gone. The P1.9 dependency proved already satisfied. Status COMPLETED |
| story/P1.15.md | [2026-09-07] Orderly shutdown — `shutdown()` on every actuator, bounded join with a log line for a thread that overruns it, and an endpoint to stop the application cleanly. Drafted as P1.14 on the P1.9 verification branch; renumbered on rebase |
| story/P1.16.md | [2026-09-07] Crash recovery — requeue documents left in a transient status on startup, with an age threshold and a vault check so live or already-indexed work is not re-processed. Drafted as P1.15; renumbered on rebase |
| story/P1.17.md | [2026-09-07] Spike: a unified composer — one screen with a text area, a `[+]` attach button and automatic URL detection, replacing the three ingest panels and the chat panel. Names the routing question (capture versus converse) as the thing the spike exists to settle, and sequences it before P1.13 because the composer constrains what those endpoints accept. Its ADR is numbered 27, ADR26 being claimed by P1.11's plan<br>[2026-09-07] COMPLETED, with an outcome section at the top. Closed as a decision rather than as a spike: no prototype was built, so the "options A and B both built against stubs" criterion is waived rather than met, and the file says so. Decision recorded as ADR27; the other five criteria are met inside it. Two gaps logged as DD-2 and DD-3 |
| story/P1.19.md | [2026-09-08] Created. Template cache off — `spring.thymeleaf.cache: false` in `application.yaml`, so a template edit shows on refresh instead of needing a restart. Found during P1.18, where a running application kept serving the previous page after the change had built, because the engine parses each template once for the life of the process. Records the decision to set it unconditionally rather than behind a `dev` profile, and what would reverse that; notes that the loop is still edit → build → refresh, because `bootRun` serves from `build/resources/main/templates/` rather than from `src/`. Touches the `spring:` block, which none of the four parallel Phase 2 plans owns |
| story/P1.20.md | [2026-09-08] Created. Live document status in the receipt feed — P1.18's receipt box grows from one line written at submission into a strip showing the states the document moves through. Closes DD-2, on its own rather than as an addition to P1.12. NOT-READY, reserving **ADR32**, and the ADR turns on something the debt entry did not anticipate: `document.status` holds only where a document is now and no transition history exists, so a polled endpoint cannot show the states a document *went through* — a stage completing in under a millisecond is invisible between two polls. Three shapes weighed (poll, SSE, a transition table), with the recommendation that the record and the delivery are separate questions. Records two collisions: DD-1 means a publish hook on either status-writing service fires for some transitions and not others, and DD-4 means a failed stage still stamps its participle, which a live strip promotes to a green tick in front of the user. Also names the parent/child problem — a URL capture's receipt ends at `clipped` while the work continues in a child document |
| story/P1.21.md | [2026-09-08] Created. Spike: generated tracking documents — design an agent and a per-entity record format so `BACKLOG-TRACKER.md` and **this file** are generated rather than hand-edited by every agent. Reserves ADR33. Premise is that the collisions are a granularity problem rather than a git problem: one file holding thirty-five independent facts makes git merge edits that never semantically overlapped, and splitting it into one record per entity makes the writes disjoint. Six questions, two load-bearing — whether the generator is a deterministic script or an LLM writing prose (the latter churns the whole file every run and brings the collisions back worse), and what happens to the dozen paragraphs of genuine argument in the tracker that no record can derive. Asks first whether story-file YAML headers can serve as the records outright, since they already carry `status`, `depends-on` and `blocks`. Acceptance turns on a fidelity check: diff the generated tracker against the hand-maintained one and account for every difference. Notes the spike traces to no Epic, because it is tooling and every phase in `PROJECT_4thBrain.md` is product<br>[2026-09-08] Version 1.1 — Q1 settled as **one shared SQLite database across all projects**. Version 1.0's file-per-entity-in-repo recommendation is superseded rather than argued down: it answered a narrower question, since a directory inside `4thBrain` cannot hold another repository's records and there are thirteen project directories under `~/.claude/projects`. The reviewability objection survived and was answered — the reviewed diff is the regenerated Markdown committed to git, not the database. Two arguments that had been missed now favour SQLite: concurrent writes become a transaction rather than a merge, which is the problem the story exists to fix, and there is no new dependency (`sqlite3.exe` 3.53.4 and Python stdlib `sqlite3` are both present). A JSON store and a spreadsheet were weighed and rejected, the spreadsheet because a single binary file is the collision problem again with no line-level merge. Q1b (where the database lives, and rebuilding it from the committed Markdown when it is lost) and Q1c (what a project key is, given worktrees and subdirectories) are new and open; acceptance gained a rebuild check and a concurrency check<br>[2026-09-08] Version 1.2 — the tooling has its own repository, **Discrepo** at `C:\Users\rsant\desar\github\Discrepo`, created with a charter and no implementation; its Phase 0 is this spike. Q1b–Q6 restated there as D2–D7 so the two question lists cannot drift apart, with this story staying where they are answered. Records that ADR numbering is per project — 4thBrain's ADR33 covers only its decision to depend on Discrepo, and Discrepo's own series starts at ADR1 — and that the follow-on implementation story belongs to Discrepo while 4thBrain keeps the adoption work and the `.claude/rules/` rewrites |
| story/P2.1.md | [2026-09-07] OllamaClient and ConcurrencyGate for bounded LLM concurrency (Semaphore(1))<br>[2026-09-08] COMPLETED. Implemented by an agent in its own worktree and merged into `v04` first of three. New `com.fourthbrain.llm` package — `OllamaClient`, `OllamaHttpClient`, `ConcurrencyGate`, `OllamaException` — plus three timeout keys on the `ollama:` block, and 11 tests. Records one re-mapped criterion (injection into `Classifier` belongs to P2.4, and no `Briefing` class exists) and one outstanding gap: the client has never called a real Ollama, because the agent could not start the application |
| design/ADR29-ingestor-boundary.md | [2026-09-08] Created by Story P2.2's design gate. Settles what the Ingestor is for: it does **not** scan `$RAW_DIR` — ADR26 makes `startChain` the only entry point, the actuator is a queue consumer with no timer, and P2.7 owns directory watching — so `raw` becomes the archive destination instead. Sets the routing table, whose load-bearing row sends text to the **Classifier** rather than the Indexer; convertible binary formats stop with a warning naming the format until P2.3 exists, rather than parking at an Extractor that only unzips. Standalone file: three agents wrote ADRs concurrently and could not all append to `ADRS.md`. **Still to be folded into `design/ADRS.md`**<br>[2026-09-08] Decision 4 amended by BUG-005 and the ADR reversed on it: text routes to the `Indexer`, and the `Classifier` runs after it as the terminal stage, so the chain is `Ingestor → Indexer → Classifier`. The heading and the two alternatives arguing for the old order were rewritten rather than left standing, since the file has still not been folded into `ADRS.md` and folding a superseded decision was the worse option. Records why the reversal wins — the vault write is the step whose failure loses a document and should not sit behind a model call — and what it costs, a window in which a published file has no topic or tags |
| design/ADR31-classification-contract.md | [2026-09-08] Created by Story P2.4's design gate, and **clears DD-3**: the Classifier produces tags, manual tagging stays possible and unbuilt. No `classification` table — the result is `document.topic` plus `document_tag` rows, because `schema.sql` is shared and a migration from one of four concurrent branches is the worst place for one. The parser takes the first balanced `{...}` by tracking brace depth, since a lazy regex returns a prefix that is still valid JSON with the tags missing. A parse failure is not a pipeline failure, deliberately the opposite of ADR28's choice for conversions. Standalone file, **still to be folded into `design/ADRS.md`**<br>[2026-09-08] Decision 4 amended by BUG-005 for the reversed chain `Ingestor → Indexer → Classifier`. The Classifier is terminal and returns null on all five paths instead of `"Indexer"`; its guarantee that a failed classification must not strand a document is unchanged but is now a property of the ordering rather than a rule five return statements had to keep. Consequences gained the publication window the reversal opens and lost the claim that the Indexer reads the topic downstream |
| story/P2.2.md | [2026-09-07] Ingestor real logic — read files from RAW_DIR, create Document records, route to TextExtractor<br>[2026-09-08] COMPLETED. Merged second. **ADR29** written as its design gate, striking the story's `$RAW_DIR` scan and routing text to the **Classifier** — a stage registered, threaded and unreachable until now. Captured notes are materialised to disk so they finally reach the vault; the original is archived to `raw`, which nothing had ever written, though `VaultArea.RAW` and `vault.raw` were both defined; PDF and the other convertible formats stop with a warning naming the format rather than being indexed as unparsed bytes. `IngestorRoutingTest` grew from 4 tests to 20. Departure recorded: metadata is persisted through a fresh `Document` carrying only name, extension and mime type, because `updateDocument` would have merged a stale in-memory status over the run loop's write |
| story/P2.3.md | [2026-09-07] TextExtractor real logic — document conversion to Markdown; extraction stack TBD by P2.8 spike<br>[2026-09-07] Rewritten against ADR28. The v03 Node stack (OpenDataLoader, Turndown, Mammoth) is struck; the story now consumes the `MarkdownConverter` seam, archives the original before conversion, and carries P2.9's failure-reason-to-status mapping as a binding contract. Blocker changed from P2.8 (a decision) to P2.9 (a component). Records that no acceptance criterion may assume Markdown headings come back from a PDF. Version 2.0 |
| story/P2.4.md | [2026-09-07] Classifier real logic — LLM tagging via OllamaClient, create Classification and DocumentTag records<br>[2026-09-08] COMPLETED. Merged third. **ADR31** written as its design gate, **clearing DD-3**. No `classification` table: the result lives in `document.topic` plus `document_tag` rows. 33 new tests (24 parsing and normalisation, 9 behaviour). Two things flagged for follow-up: `OllamaClient` is injected `required = false` as a merge-order workaround that should now be tightened to a required injection, and classification has never run against a real model — every test uses a hand-written fake |
| story/P2.5.md | [2026-09-07] Indexer real logic — vault storage and Smart Connections MCP indexing |
| story/P2.6.md | [2026-09-07] Briefing real logic — scheduled synthesis of recent documents via OllamaClient |
| story/P2.7.md | [2026-09-07] File watcher — monitor RAW_DIR for automatic ingestion via WatchService or polling |
| story/P2.8.md | [2026-09-07] Spike: MarkItDown vs Apache Tika as document converter; six integration options analysis; results in ADR and P2.3 rewrite<br>[2026-09-07] Status section added. Three of five acceptance criteria met — analysis, ADR28, P2.3 rewritten — and two not: no fixture corpus and no measurements of any kind. Stays WIP rather than being closed on a decision taken without them. Records the format census and the scanned-PDF split as the two commands to run next, either of which can reverse the stack |
| story/P2.9.md | [2026-09-07] MarkdownConverter seam and its MarkItDown subprocess implementation, carrying out ADR28: the three-method interface, the `com.fourthbrain.convert` package, the full conversion path including staging, stream redirection and descendant-killing on timeout, a startup probe that converts a real non-ASCII fixture rather than calling `--version`, the failure-reason-to-status mapping, 18 acceptance criteria, and eight design placeholders still waiting on P2.8's measurements |
| story/P2.10.md | [2026-09-08] Row added — the file has existed since 2026-09-07 but was never indexed. Environment story standing up Ollama on Windows: native install over WSL2, an explicitly pinned model tag, `OLLAMA_MODELS` set before the first pull, autostart, keep-alive, and a runbook at `design/OLLAMA-SETUP.md`. Verifies `/v1/chat/completions` specifically, because the app never calls `/api/generate` and a green `/api/tags` proves less than it looks like it does<br>[2026-09-08] Version 1.1 — cross-referenced to `design/SPIKE-LOCAL-MODEL-SELECTION.md`, which answers the model-choice question this story defers. Decision 2 now records that the spike's recommendation is *not* `mistral` and what to pin under either outcome; decision 3 gains the 4B size figure; decision 5 gains `OLLAMA_NUM_PARALLEL=1` alongside the keep-alive; step 8 asks for memory *speed*, since DDR4 versus DDR5 selects which column of the spike's throughput table applies. Out of Scope keeps model quality out but now names where it is argued, and records the gap the spike cannot close — no model output has ever been compared against a human answer, P2.4 having closed with every test using a hand-written fake, and no story owns measuring it. `response_format: json_schema` and a capped `num_ctx` explicitly held out as production code<br>[2026-09-08] Version 1.2 — the Out of Scope bullet that recorded "a story that measures tag quality does not exist yet" now names [[P2.11]], which does. Decision 2 notes P2.11 may change the pin again on evidence; decision 3 warns that P2.11 pulls three models, so the storage estimate is for a set rather than one; `blocks` gains P2.11<br>[2026-09-08] Version 1.3 — **COMPLETED**, resolving the contradiction where `BACKLOG-TRACKER` listed it complete and this file's header said READY. Status section added carrying the verification (Ollama **0.33.3**, service answering on 11434, `devstral:24b` and `gemma4:12b` installed) and marking each acceptance criterion met or not: two of six. The one that matters is a defect rather than a documentation gap — `application.yaml` names `mistral`, which is installed nowhere, so the first real Classifier call gets a model-not-found, invisible until now because P2.4's tests all use a fake client and P2.1's has never called a live Ollama. Three criteria carried: no runbook, no `/v1/chat/completions` timing, no reboot check. Each of the five decisions now records what was actually settled, and decision 2 (the model pin) is explicitly handed to [[P2.11]] |
| story/P2.11.md | [2026-09-08] Created. Measures what nothing in the project has ever measured — whether the Classifier's tags are *right*. P2.4 closed with 33 tests and every one of them runs against a hand-written fake, so they prove parsing and normalisation and say nothing about output quality; `application.yaml` names `mistral` because someone wrote it down first, never because it won anything. Exists because `design/SPIKE-LOCAL-MODEL-SELECTION.md` argues for replacing that model on hardware grounds alone, and if the smaller model tags worse nothing currently on file would reveal it. Reserves **ADR34**. Five decisions, two load-bearing: what counts as a correct tag (hand-labelling recommended over inter-model agreement, which measures consensus — three models sharing a bias agree and are all wrong together — and over retrieval, which needs search that is not built), and whether `response_format: json_schema` is a variable or a constant, which decides whether the experiment is a grid or a list. Also asks where the harness lives, since a `@Disabled` JUnit class reads better and drags a live Ollama into a tree `gradle build` runs on every branch. Gives ADR31's accepted "tag vocabulary drifts" consequence a number, and re-uses P2.8's never-run format census so the fixture set matches real inflow. Holds the prompt fixed, because changing prompt and model together makes both results unreadable<br>[2026-09-08] Version 1.1 — **READY**, unblocked by P2.10's completion, and larger for it. P2.10 closed without pinning a model, so this story no longer only chooses between candidates: it produces the first tag `application.yaml` can actually resolve. The candidate set changed with it — no incumbent arm, since pulling `mistral` to defend a choice nobody made is wasted time, and `gemma4:12b` joins as the upper bound because it is already on disk. `devstral:24b` is noted and excluded, being a coding model. Acceptance now requires the chosen tag to appear in `ollama list`, which is the check P2.10 did not make |
| design/ADR35-runtime-model-resolution.md | [2026-09-08] Created as Story P2.12's design gate and carried out in the same pass. The model is whichever one Ollama has loaded, read from `/api/ps` before each call; `ollama.model` is removed rather than corrected, so the configuration can no longer name something that does not exist. Five decisions. Per-call rather than cached, because a cached answer makes a mid-session `ollama run` swap invisible and attributes a result to a model that did not produce it — the one thing P2.11's measurements must not do. The native root is derived by stripping `/v1` instead of gaining a second key, since two keys that must agree will stop agreeing. Nothing loaded throws before `/chat/completions` is contacted, rejecting the implicit-load alternative that produced the original defect and that would surface on this hardware as a 120-second read timeout rather than a diagnosis. Two loaded uses the first and warns, rejecting an ambiguity error as too brittle for the comparison workflow. `isAvailable()` re-defined as "a model is loaded", replacing a probe that returned true in exactly the failing case. Records that **ADR31 is deliberately untouched** — a failed classification stays non-fatal, so nothing loaded means untagged documents rather than a stopped pipeline — and that nothing auto-loads any more, so a reboot leaves every LLM call failing with no startup warning. Standalone file, **still to be folded into `design/ADRS.md`** alongside ADR29, ADR30 and ADR31 |
| story/P2.12.md | [2026-09-08] Created and **COMPLETED** in the same pass, carrying out ADR35. Closes the defect P2.10 surfaced — `application.yaml` naming `mistral` against a machine that has `devstral:24b` and `gemma4:12b` — by removing the configured model name entirely rather than fixing its value. Three files changed: `OllamaHttpClient.java`, `application.yaml` (the key deleted, with the reason left as a comment where it stood) and the test class. `gradle build` green at 106 tests, 0 failures, 2 skipped (`PipelineFlowTest`, unrelated, disabled against BUG-005); `OllamaHttpClientTest` 6 → 12, covering the nothing-loaded path proving no request reaches `/chat/completions`, the two-loaded path, the `name`/`model` field fallback, and the derived native root. The `/api/ps` response shape was checked against the running server rather than assumed. **No live round trip** — every test is against `MockRestServiceServer`, the same gap P2.1 closed with, and exercising it needs a model that fits the machine, which is P2.11's job. Holds out of scope a startup probe (what the application should *do* about nothing being loaded is its own design question) and any change to ADR31's failure semantics |
| story/P3.1.md | [2026-09-07] Unit tests for actuators (JUnit 5, Mockito, >80% coverage of process() logic) |
| story/P3.2.md | [2026-09-07] Integration tests for full message flows through actuator chain (multiple stages, concurrency) |
| story/P3.3.md | [2026-09-07] REST API tests via MockMvc for all endpoints (file/text/URL ingestion, status, search, chat) |
| story/P3.4.md | [2026-09-07] Manual smoke test — browser UI, file upload, watch pipeline progress through dashboard |
| story/P3.5.md | [2026-09-07] Upgrade to JUnit 5 (Jupiter) — migrate all tests from JUnit 4, replace annotations and lifecycle<br>[2026-09-07] Rewritten against what the tree actually contained: the migration had already happened, and the story's real subject is a dangling `junit:junit` on the test classpath with no vintage engine, which turns a JUnit 4 test into a silent zero-test pass. Status section added recording the removal, the `@MockBean` → `@MockitoBean` change folded in, and the four verifications. Status COMPLETED |
| bug/BUG-001.md | [2026-09-07] Web UI fails to load on localhost:8080 or localhost:8080/chat; UIController routing or static resources issue; blocks P1.13 and P3.4<br>[2026-09-07] Diagnosed against the code: UIController and AdminController are `@Controller`s returning view names, and build.gradle carries no template engine, so the names resolve to nothing; `@GetMapping("/")` also suppresses the welcome page that would otherwise serve static/index.html. Missing CSS/JS and static-resource config ruled out. Three fix options recorded, none chosen<br>[2026-09-07] Fix decided: Thymeleaf (ADR25), carried out by Story P1.14. Recorded that the cheaper option was the better one on the evidence and was passed over for roadmap reasons, so the tradeoff survives review. Status READY<br>[2026-09-07] Closed by P1.14 and verified against a running application |
| bug/BUG-002.md | [2026-09-07] A fresh clone will not start: `application.yaml` points SQLite at `data/fourthbrain.db`, SQLite creates the file but not its parent directory, and `data/` is untracked. Found during P1.14 verification, where it masked P1.9's true status. Three fix options; `vault/` and `tmp/` have the same gap |
| bug/BUG-003.md | [2026-09-08] Created. Below 768px the left nav disappears and nothing brings it back, so Search, Ingest status and Admin become unreachable. Two independent causes, either fatal on its own: the hamburger carries an inline `display:none` that outranks the media query meant to reveal it, and no JavaScript has ever referenced `menu-btn` or toggled `.sidebar.open`, so that rule has never had a way to match. Predates P1.18 — P1.7 skeleton markup that P1.18 kept byte-identical — and P1.18 reduced the impact rather than causing it, since the composer is now the panel active on load. Three fix options; A removes the inline style rather than out-specifying it<br>[2026-09-08] COMPLETED via option A. The eight declarations moved from the inline `style` attribute into a `.menu-btn` rule in the `<style>` block, so the `max-width: 768px` rule revealing the button is no longer outranked, and `wireSidebarToggle`/`setSidebar` toggle `.sidebar.open` and close it again when a nav item is chosen. Records how far each criterion was verified: the two structural ones by reading the file, the four describing browser behaviour on the code being right, since nothing here renders a page |
| bug/BUG-004.md | [2026-09-08] Created and closed in the same pass. An upload declared `application/octet-stream` lost its extension and stopped at `ingested`, never reaching the vault. Three chained defects in `IngestionController.processUploadedFile`: octet-stream is not blank so the `isBlank` guard accepted it and the `probeContentType` fallback was dead code for the case it was written for; the extension was derived from the MIME type rather than the filename, so it was discarded; and `Ingestor` checks mime then extension, matched neither, and parked the document. Fixed by taking the extension from `getOriginalFilename()` and resolving the type in a stated order, with parameter stripping and an extension-to-type table covering ADR28's converter formats and the archives routed to the `Extractor`. Evidence was in the working database and in 23 extensionless files in `tmp/`. Records two things held out of scope — the table duplicates `Extractor.guessMimeType`, which P2.3 rewrites, and `newName` computes uniqueness before the extension is appended, which `plan-P2.2.md`'s `VaultNaming` should settle |
| bug/BUG-005.md | [2026-09-08] Created. The full-path chain runs Classifier before Indexer: the required order is `Ingestor → Indexer → Classifier` and the merged code does `Ingestor → Classifier → Indexer`. Not a defect in any one class — each actuator matches its own design, and the disagreement lives in **ADR29** (which changed the Ingestor's text route) and **ADR31**. Records that no test covered the chain before `PipelineFlowTest`, because `IngestorRoutingTest`, `ClassifierTest` and `IndexerTest` each assert one actuator's decision in isolation and all three pass while the chain they compose is wrong. Names four things the reversal has to decide rather than just change, chiefly that a file would reach the vault before it carries a topic, which matters to whatever watches the vault under ADR30<br>[2026-09-08] COMPLETED. All six fix steps taken plus the decision under point 2: the chain is `Ingestor → Indexer → Classifier`, ADR29 and ADR31 amended, `PipelineFlowTest` enabled, the three actuator tests updated, and `Indexer` stopped logging `topic=` now that it is structurally null. 100 tests, 0 skipped. The publication window point 1 names is left open and recorded as belonging to whichever story wires an external indexer |
| design/ADR30-java-mcp-transport.md | [2026-09-08] Created by Story P2.5's Part B, status **Proposed, not decided** — the plan's experiment could not be run without starting the application. Reading the installed server settled three options anyway: `@yejianye/smart-connections-mcp` speaks stdio only with no listener, and its four tools (`lookup`, `connection`, `stats`, `validate`) are all read-side, so nothing indexes, embeds or writes. That eliminates an HTTP endpoint and makes a hand-rolled or SDK MCP client a transport to a server with no call on the far end, leaving option A — the vault write is the trigger — as the only mechanism the installed stack offers. Records the experiment that would confirm it, and that the vault path mismatch would make it fail today for an unrelated reason. **Still to be folded into `design/ADRS.md`** |
| design/SPIKE-LOCAL-MODEL-SELECTION.md | [2026-09-08] Created. Answers the model-choice question Story P2.10 defers, for one machine: Windows, 32 GB RAM, no GPU. Finding is that parameter count outranks bit depth — with no VRAM the limit is memory bandwidth rather than capacity, so 32 GB would hold a 32B model that runs at one to two tokens per second. Q4_K_M for anything at 7B or above; the current `mistral` tag is the older Q4_0, so the quantisation change is free. Recommends a 4B-class model, which is *smaller* than what the project names today, because Mistral 7B dates from 2023 and a 2025-era 4B beats it on instruction-following and structured output at half the size. Records that the Classifier's cost is prefill and not generation, since ADR31's output is ~40 tokens against a whole document of input. Two settings called out as mattering more than the model: `response_format: json_schema` on the OpenAI-compatible surface `OllamaHttpClient` already calls, which constrains decoding so ADR31's balanced-brace parser stops firing, and a capped `num_ctx`. Embeddings weighed as the alternative and rejected on ADR31 — an embedding model can only select from a vocabulary, and ADR31 decided the Classifier produces one<br>[2026-09-08] Version 1.1 — opens by stating that nothing in it is a measurement, and hands the "What to measure" section to Story **P2.11**, which now owns those four numbers and puts them in ADR34 rather than in a later version of this file. Records which of the four can reverse the recommendation (tag agreement, item 4) and why it is the expensive one. The embeddings alternative gains the link that would tell anyone whether its moment has arrived — P2.11 measures the vocabulary drift ADR31 accepted without a number. Closes with the intended order: pin something so behaviour stops drifting, measure, re-pin once |
| simple-claude-plan.md | [2026-09-07] Audit of the Claude governance layer — the nine rules, eight project skills, the solving-loop agent, settings.local.json and the tracking documents. Ten findings, eight referenced files that do not exist, and a three-pass plan cutting 9 rules to 5, 8 project skills to 0, four status-tracking places to 1 and 77 permission entries to about 8 |
=======
---
name: INDEX
description: Index of design documents, execution plans, stories and bugs under documents/ in 4thBrain v04
metadata:
  version: 2.13
  created-by: Claude Sonnet 5
  date: 2026-09-08
---

# INDEX — 4thBrain v04 documents

Catalog of everything under `documents/`, subdirectories included. Paths are relative to this
file. The repository root and `scripts/` carry their own INDEX; see
[../INDEX.md](../INDEX.md).

| File Name | History |
|-----------|----------|
| design/SYSTEM-REQUIREMENTS-SPECIFICATION.md | [2026-09-03] Functional Requirements FR1–FR9, Non-Functional Requirements NFR1–NFR12 (reference to v03, no changes)<br>[2026-09-07] No file exists at this path in v04 — the specification lives in v03 and is referenced, not copied. Row kept as the pointer to it |
| design/ADRS.md | [2026-09-03] Architecture Decision Records (reference to v03: ADR14, ADR17, ADR24)<br>[2026-09-07] File actually created — it had been indexed and cited since 2026-09-03 but never written. Records that ADR1–ADR24 stay inherited from v03, and adds ADR25: Thymeleaf is the view layer, served pages live in `templates/`, `static/` holds only linked assets. Written up with the counterargument (it buys nothing today) and the `${...}` JavaScript-vs-Thymeleaf trap<br>[2026-09-07] ADR28 added — MarkItDown invoked as a subprocess is the Extractor's Markdown converter, closing Story P2.8's decision criterion. Decided on the argument rather than on the corpus measurements, which have not been run, and says so: the case is that a hard timeout only exists across a process boundary, and this pipeline makes a stalled document invisible because it is driven by `Document.status`. Records that on PDF both candidates are flat text extractors, so "built for LLM consumption" cannot carry the argument; carries three falsifiable triggers, two of which reverse the decision. Numbered 28 because ADR26 is reserved by P1.11's plan and ADR27 by P1.17's spike<br>[2026-09-07] ADR26 written, closing the reservation: `startChain` is the sole entry point, a message carries the fully-loaded `Document` rather than an id (so no actuator re-fetches one it already holds), `Message.from` is null-safe, the Coordinator registry becomes instance state, a submitted URL lives in `source_url`, `DatabaseService` alone writes `Document.status`, and `/api/ingest/text`/`/url` collapse into one `POST /api/ingest/capture` dispatching on which body key is present — extending the decision from P1.11 into P1.13. ADR28's footnote corrected to show only ADR27 as still unwritten<br>[2026-09-07] ADR27 written, closing the last reservation: one composer shaped like a chat box, `[+]` `ASK` `URL` `SEND` inside the container, the user naming the action and detection only enabling the `URL` button — which lights only when the whole trimmed input is a single `http`/`https` URL, so `SEND` on a bare URL storing it as text is the override. Text plus an attached file is two submissions and two Documents; several files are one Document each; no tags field; Chat folds in, so the nav drops from six items to four. Records that it was taken without the prototype Story P1.17 called for, and what would reopen it. ADR28's footnote updated again — both reservations are now written |
| design/SPIKE-MARKITDOWN.md | [2026-09-07] Story P2.8 brief: what MarkItDown is (MIT, Python ≥ 3.10, CLI / library / MCP server, formats and their underlying parsers), what it would and would not replace in the pipeline, six options for calling an external Python process from Spring Boot compared side by side, Apache Tika as the no-Python comparison arm, what the spike must measure, exit criteria, risks, one-day timebox |
| design/SPIKE-UNIFIED-COMPOSER.md | [2026-09-07] Story P1.17 brief: collapse the Add File, Add Text, Add URL and Chat panels into one composer; five options for telling capture from conversation in a single input (mode toggle, two send buttons, sigil, inferred intent — rejected, and ingest-only unification as the fallback); a narrow URL detection rule firing only when the whole input is one URL; attachment, tagging and receipt-feed questions; the three endpoints still stubbed behind the screen; ADR25 constraints; exit criteria and a half-day timebox |
| design/STARTUP-SEQUENCE.md | [2026-09-06] Story P1.9 design: bean initialization order, ActuatorManager, three phases (create+inject+name, register all, start all), why the current sequence aborts, sequence diagram<br>[2026-09-06] Rewritten as built rather than proposed: registry documented as ConcurrentHashMap of CopyOnWriteArrayList with `instancesOf`, `assignName` explained, shutdown section updated for the working `shutdown()` and the interrupt break, configuration section states what is in application.yaml, and the closing note on the removed `Actuator.map`/`registry` fields folded into the P1.10 section |
| P1.11-FIX-PLAN.md | [2026-09-07] Execution plan for P1.11: story claims verified line by line, two corrections (the Message NPE is latent, and `sendMessage` has a second unguarded dereference) and two further defects found (`Message.builder()` never stamps `createdAt`; `id()` misspells "Mesasge"). Design gap flagged — an ADR required before code, because clearing a static registry on context close does not give test isolation under Spring's context cache<br>[2026-09-07] `UI-ENTRY-POINTS-PLAN.md` merged in and deleted, so one plan covers the run rather than two covering the same stories. Now four staged: P1.11 (entry point), P1.17 (composer spike, in parallel), P1.13 (text and URL endpoints), P1.18 (the composer itself, story still to be created). New finding carried in: P1.13 cannot meet its own acceptance criteria as written, because `Ingestor.isUrl` and `Clipper` both read a submitted URL from `content` rather than `source_url`, so a document built as the story describes stops at the first hop with no error. Correction to the merged plan's premise: it claimed `DocumentService.getDocumentById` and `setStatus` do not exist — both do, at `DocumentService.java:40` and `:105`. The sketch still uses `DatabaseService`, because that is the synchronized service under ADR17 and the one `IngestionController` injects, which surfaces a real inconsistency the plan now logs — two services write `Document.status` and only one is synchronized. Filename kept though it is narrower than the contents; version 2.0<br>[2026-09-07] Stage 3 rewritten against ADR26 decision 6: `/text` and `/url` collapse into one `POST /api/ingest/capture`, dispatching on whichever of a `url` or `text` body key is present, rather than staying two endpoints. Design-decisions list gained this as item 6, noting ADR26 has since been written. Acceptance-criteria mapping row for P1.13 split to add "`/text` and `/url` no longer exist". Version 2.1 |
| plan-11-13-17.md | [2026-09-07] Row added — the file existed and was referenced from `BACKLOG-TRACKER.md` but had no INDEX entry. Alternative plan delivering P1.11, P1.13 and P1.17 as one change rather than four staged deliveries; not yet chosen over `P1.11-FIX-PLAN.md`<br>[2026-09-07] Step 5, step 6 and the acceptance-criteria mapping updated to match ADR26 decision 6: `/text` and `/url` collapse into one `POST /api/ingest/capture`. Notes that ADR26 (its own step 0 draft) has since actually been written; only ADR27 remains open. Version 1.1 |
| plan-P1.18.md | [2026-09-07] Execution plan for P1.18 (The Unified Composer). Owns `index.html` alone and touches no Java, so it merges independently of the other four plans in the set. No new ADR — ADR27 settles the design. Seven steps: nav from seven items to four, the composer container and its CSS, ADR27's URL rule implemented as stated, four submit handlers, an escaping feed, deletion of the four superseded panels and their JavaScript, and ADR25's constraints restated. Carries a found defect: the chat panel interpolates user text into `innerHTML` unescaped, and the rewrite must not copy that into the feed. Ten manual verification steps including ADR27's URL truth table; DD-2 and DD-5 recorded as carried rather than closed<br>[2026-09-07] Version 1.1 — `review-plan-p1.18.md` folded in, with a disposition table recording six findings taken and one rejected. Three were real defects in v1.0: the plan said files upload on attach and also that they stage, which contradict; re-enabling the buttons in a `finally` would have lit `URL` against ADR27's rule, so all state now goes through one `updateComposerState()`; and blanket-clearing after a send destroyed the user's note when one of several uploads failed, so nothing is cleared until its own request returns 2xx. Rejected: the claim that `/api/ingest/file` omits `status` — it returns it at `IngestionController.java:139`, and the review quotes a four-key map that does not match the file. Also gained an `ASK` prompt bubble, labelled receipts, `white-space: pre-wrap`, a drag counter, textarea auto-grow, and an optional `Ctrl+Enter` accelerator marked as going past ADR27. Verification grew from ten steps to fifteen |
| review-plan-p1.18.md | [2026-09-07] Technical review of `plan-P1.18.md` against the codebase: three state and logic contradictions, one claimed contract mismatch, and several interaction traps, with drop-in CSS and JavaScript. Folded into `plan-P1.18.md` v1.1, where its disposition table records six findings taken and finding 1.4 rejected — `/api/ingest/file` does return `status` |
| plan-P2.1.md | [2026-09-07] Execution plan for P2.1 (OllamaClient & ConcurrencyGate). Creates a new `com.fourthbrain.llm` package no other plan opens, and publishes the `OllamaClient` interface `plan-P2.4.md` codes against byte-identically. No new ADR. Departs from the story in one place: it does not inject into `Classifier` (that file is P2.4's) and `Briefing` does not exist, so that criterion re-maps. Records that `ollama.url` already ends in `/v1`, that the gate needs a `tryAcquire` timeout or a hung model parks an actuator thread invisibly, and that P2.10's status is disputed between the tracker (COMPLETED) and its own story header (READY) |
| plan-P2.2.md | [2026-09-07] Execution plan for P2.2 (Ingestor Real Logic), gated on **ADR29**. Two questions the ADR must answer: whether the Ingestor reads `$RAW_DIR` (recommended no — ADR26 makes `startChain` the only entry point and P2.7 owns directory watching), and what it does instead. Three findings drive the answer: a captured text document has no file and no copy row, so a typed note reaches the Indexer and stops there; nothing writes to the `raw` area though ADR28's failure semantics assume the original survives; and the routing table sends text past the Classifier, which is registered, threaded and unreachable. Steps materialise content to disk, archive the original, validate, and route from the ADR's table |
| plan-P2.4.md | [2026-09-07] Execution plan for P2.4 (Classifier Real Logic), gated on **ADR31**, which closes DD-3. Four decisions: the Classifier produces tags (manual tagging stays possible and unbuilt); no `classification` table — `document.topic` plus `document_tag` rows, because `schema.sql` is shared with three parallel branches; the parser extracts the first balanced `{...}` so prose- and fence-wrapped replies work; and a parse failure is not a pipeline failure, which is deliberately the opposite of ADR28's choice for conversion failures. Reproduces P2.1's `OllamaClient` interface verbatim so the branch compiles if it runs first |
| plan-P2.5.md | [2026-09-07] Execution plan for P2.5 (Indexer Real Logic), in two parts. Part A is unblocked and fixes two defects: two documents with the same file name silently overwrite each other in the indexing area, and a captured text document never reaches the vault at all. It writes atomically through a `.part` rename without touching the frozen `DatabaseService`. Part B is blocked on **ADR30**, the "how does Java talk to MCP" question the story itself flagged and ADR28 explicitly declined to answer; the plan recommends running a ten-minute experiment first, on the theory that Smart Connections indexes on the file write and there is no call to make. Records that `vault.path` points inside the repository rather than at the Obsidian vault |
| DESIGN-DEBT.md | [2026-09-07] Row added — the file existed since the P1.11/P1.13 implementation but was never indexed. Open design gaps found mid-plan and logged rather than resolved in the same pass, per `.claude/rules/design-before-implementation.md`. Opened with DD-1: two services write `Document.status` and only `DatabaseService` is synchronized<br>[2026-09-07] DD-2 and DD-3 added from ADR27. DD-2: no endpoint reports the status of one document, so the composer's receipt shows what the server returned at submission and never updates; P1.12 was left alone rather than widened from the outside. DD-3: tags have no owner once the ingest panels are gone, and whether they are typed or produced by the Classifier belongs with P2.4's design<br>[2026-09-07] DD-4 and DD-5 added while planning the five parallel stories. DD-4: `Actuator.run()` stamps the participle unconditionally after `doTheThing` returns, so an actuator has no way to record a failure and a document that failed reads exactly like one that succeeded — which contradicts ADR28's own failure semantics and is tangled with DD-1 in the same three lines. DD-5: `/api/chat/llama` is still the Phase 1 echo stub and no story owns wiring it, so ADR27's `ASK` button returns the stub even after both P1.18 and P2.1 land. Notes added to DD-1 (all five plans freeze `Actuator.java`, so none can close it) and DD-3 (`plan-P2.4.md` is written to close it via ADR31)<br>[2026-09-08] Notes added to DD-2 and DD-4 from Story P1.20. DD-2 records that P1.20 is written to close it and that designing it surfaced a harder constraint than the entry assumed — with no transition history anywhere, a polled endpoint cannot show the states a document went through, only where it is now. DD-4 records that a live status strip raises its stakes, turning a failed stage's wrongly-stamped participle from an unread column value into a green tick on screen<br>[2026-09-08] **DD-3 marked CLEARED** by ADR31, written as Story P2.4's design gate and merged with it: the Classifier produces tags, manual tagging stays possible and unbuilt, and the Markdown-heading trap does not fire because tags arrive as JSON array elements rather than inline `#tokens` — recorded so a later inline-tagging story knows it inherits the trap intact |
| done/P1.9-FIX-PLAN.md | [2026-09-06] Execution plan for P1.9: ActuatorManager/Coordinator code already implemented but unverified; three defects found (unsynchronized registry read, null next-actuator dereference, spurious-interrupt hot spin); new ActuatorManagerTest; boot verification steps against acceptance criteria<br>[2026-09-06] Plan executed in full; all six P1.9 acceptance criteria verified<br>[2026-09-07] Filed under `done/` once the plan completed; the row had been indexed at `documents/P1.9-FIX-PLAN.md`, where the file no longer sits |
| done/P3.5-UPGRADE-PLAN.md | [2026-09-07] Execution plan for P3.5: the JUnit 5 migration had already happened before the story was written — both test classes are Jupiter, no `@RunWith`/`@Rule`/`org.junit` import exists. What remains is deleting `junit:junit:4.13.2` from build.gradle, which sits on the test classpath with no vintage engine, so a JUnit 4 test compiles and is then silently not executed. Adjacent `@MockBean` → `@MockitoBean` finding recommended for the same pass; verification is a baseline-and-compare on executed test count<br>[2026-09-07] Filed under `done/` once the plan completed; the row had been indexed at `documents/P3.5-UPGRADE-PLAN.md`, where the file no longer sits |
| story/P1.8.md | [2026-09-07] Remove document.path, add document_copy table and source_url field; vault areas (tmp/incoming/indexing/raw) |
| story/P1.9.md | [2026-09-07] Actuator instantiation and registration — single owner (ActuatorManager), ordered lifecycle, fixing double-instantiation NPE<br>[2026-09-07] Status section added: implemented and verified 2026-09-06 by boot log and `ActuatorManagerTest`, with the three defects found while verifying. Status COMPLETED |
| story/P1.10.md | [2026-09-07] Actuator run loop — blocking queue, ordered shutdown, bounded performance, fix busy-spin and core usage<br>[2026-09-07] Status section added: landed in the same pass as P1.9 and verified with it; idle actuator threads use no measurable CPU. Status COMPLETED |
| story/P1.11.md | [2026-09-07] Coordinator entry point and message addressing — settle on startChain(), fix message logging NPEs<br>[2026-09-07] Change section corrected to state explicitly that `startChain` loads the document once and passes it by reference through `Message`, matching ADR26 and the run loop's existing behaviour — no per-hop database re-fetch. Status moved to READY now that ADR26 is written. Version 1.1<br>[2026-09-07] Implemented via `plan-11-13-17.md` and verified (`gradle build`/`gradle test` green, new `CoordinatorTest`). Status COMPLETED. Version 1.2 |
| story/P1.12.md | [2026-09-07] Status endpoint reports document counts from database instead of actuator queue depths |
| story/P1.13.md | [2026-09-07] Text and URL ingestion endpoints, rewrite 23 controller tests against current contract<br>[2026-09-07] Rewritten against ADR26 decision 6: `/text` and `/url` collapse into one `POST /api/ingest/capture`, dispatching on whichever body key (`url` or `text`) is present rather than the request path; the UI's own type-detection picks the key. `/file` unchanged. Now depends on P1.17 as well as P1.11, and blocks P1.18. Version 1.1<br>[2026-09-07] Implemented via `plan-11-13-17.md` ahead of P1.17's spike actually running, on the plan's argument that only ADR26's contract-bearing answer (one endpoint) gated this story, not the routing-UI decision. Verified (`gradle build`/`gradle test` green, `IngestionControllerTest` rewritten). Status COMPLETED. Version 1.2 |
| story/P1.18.md | [2026-09-07] Created. Follow-on to P1.17's spike: builds the composer the spike names, wired to P1.13's single `/api/ingest/capture` endpoint. Blocked by P1.13 and P1.17; cannot start until ADR27 is written<br>[2026-09-07] READY — both blockers cleared. Rewritten against ADR27 rather than hedged on the spike's outcome: Chat folds into the composer (four panels removed, not three), the URL rule is stated as the button-enable condition with `SEND` as its override, tags are dropped (DD-3), and per-document status is explicitly not built (DD-2). Acceptance criteria gained the `ASK` path and the four URL-detection cases. Records that the design arrives without prototype backing, so this story's first run is the evaluation the spike skipped<br>[2026-09-07] COMPLETED, with a Status section at the top. Implemented from `plan-P1.18.md` v1.1 in one file (`index.html`, +376/−189) and verified against a running app on port 8081, a pre-existing instance holding 8080. Records what was checked — the rendered page, ADR25's two constraints, all four endpoint contracts including the 400 path, and the database state behind a URL capture versus a text capture — and what stays open: ADR27's reopening question needs a person using the screen, not another pass over the code. Two gaps surfaced and assigned elsewhere: a captured text document has no file and no copy row so it never reaches the vault, and `Clipper` leaves its child document at status `New` |
| story/P1.14.md | [2026-09-07] View layer and template engine — add Thymeleaf, move index.html into templates/, replace the System.out stubs in both controllers; closes BUG-001, design in ADR25, verification waits on P1.9<br>[2026-09-07] Implemented and verified against a running application: all six endpoints return 200, the inline script's `${...}` literals survive rendering, `static/` is gone. The P1.9 dependency proved already satisfied. Status COMPLETED |
| story/P1.15.md | [2026-09-07] Orderly shutdown — `shutdown()` on every actuator, bounded join with a log line for a thread that overruns it, and an endpoint to stop the application cleanly. Drafted as P1.14 on the P1.9 verification branch; renumbered on rebase |
| story/P1.16.md | [2026-09-07] Crash recovery — requeue documents left in a transient status on startup, with an age threshold and a vault check so live or already-indexed work is not re-processed. Drafted as P1.15; renumbered on rebase |
| story/P1.17.md | [2026-09-07] Spike: a unified composer — one screen with a text area, a `[+]` attach button and automatic URL detection, replacing the three ingest panels and the chat panel. Names the routing question (capture versus converse) as the thing the spike exists to settle, and sequences it before P1.13 because the composer constrains what those endpoints accept. Its ADR is numbered 27, ADR26 being claimed by P1.11's plan<br>[2026-09-07] COMPLETED, with an outcome section at the top. Closed as a decision rather than as a spike: no prototype was built, so the "options A and B both built against stubs" criterion is waived rather than met, and the file says so. Decision recorded as ADR27; the other five criteria are met inside it. Two gaps logged as DD-2 and DD-3 |
| story/P1.19.md | [2026-09-08] Created. Template cache off — `spring.thymeleaf.cache: false` in `application.yaml`, so a template edit shows on refresh instead of needing a restart. Found during P1.18, where a running application kept serving the previous page after the change had built, because the engine parses each template once for the life of the process. Records the decision to set it unconditionally rather than behind a `dev` profile, and what would reverse that; notes that the loop is still edit → build → refresh, because `bootRun` serves from `build/resources/main/templates/` rather than from `src/`. Touches the `spring:` block, which none of the four parallel Phase 2 plans owns<br>[2026-09-08] Version 1.1 — **COMPLETED**, with a Status section at the top. Six lines added to the `spring:` block, the reason carried beside the setting as a comment; no other block touched. Verified live on port 8081 in the one order that proves the point — the page fetched and cached first, then a marker added to `admin.html`, placed in `build/resources/main/templates/` and served without a restart, then reverted and gone. 106 tests, 0 failures, 0 skipped. One criterion re-mapped: `/admin/api-docs` is not a URL this application has, `AdminController` mapping `/api/docs` so the page sits at `/admin/api/docs`; the story wrote the path down wrong and the four real URLs all return 200. Two notes added that the original could not have known: `bootRun` holds the Gradle project lock, so the edit → `processResources` → refresh loop the story describes stalls in a single directory and the copy has to be made another way, and starting in a fresh worktree still needs `data/` created by hand, which is BUG-002 unchanged |
| story/P1.20.md | [2026-09-08] Created. Live document status in the receipt feed — P1.18's receipt box grows from one line written at submission into a strip showing the states the document moves through. Closes DD-2, on its own rather than as an addition to P1.12. NOT-READY, reserving **ADR32**, and the ADR turns on something the debt entry did not anticipate: `document.status` holds only where a document is now and no transition history exists, so a polled endpoint cannot show the states a document *went through* — a stage completing in under a millisecond is invisible between two polls. Three shapes weighed (poll, SSE, a transition table), with the recommendation that the record and the delivery are separate questions. Records two collisions: DD-1 means a publish hook on either status-writing service fires for some transitions and not others, and DD-4 means a failed stage still stamps its participle, which a live strip promotes to a green tick in front of the user. Also names the parent/child problem — a URL capture's receipt ends at `clipped` while the work continues in a child document |
| story/P1.21.md | [2026-09-08] Created. Spike: generated tracking documents — design an agent and a per-entity record format so `BACKLOG-TRACKER.md` and **this file** are generated rather than hand-edited by every agent. Reserves ADR33. Premise is that the collisions are a granularity problem rather than a git problem: one file holding thirty-five independent facts makes git merge edits that never semantically overlapped, and splitting it into one record per entity makes the writes disjoint. Six questions, two load-bearing — whether the generator is a deterministic script or an LLM writing prose (the latter churns the whole file every run and brings the collisions back worse), and what happens to the dozen paragraphs of genuine argument in the tracker that no record can derive. Asks first whether story-file YAML headers can serve as the records outright, since they already carry `status`, `depends-on` and `blocks`. Acceptance turns on a fidelity check: diff the generated tracker against the hand-maintained one and account for every difference. Notes the spike traces to no Epic, because it is tooling and every phase in `PROJECT_4thBrain.md` is product<br>[2026-09-08] Version 1.1 — Q1 settled as **one shared SQLite database across all projects**. Version 1.0's file-per-entity-in-repo recommendation is superseded rather than argued down: it answered a narrower question, since a directory inside `4thBrain` cannot hold another repository's records and there are thirteen project directories under `~/.claude/projects`. The reviewability objection survived and was answered — the reviewed diff is the regenerated Markdown committed to git, not the database. Two arguments that had been missed now favour SQLite: concurrent writes become a transaction rather than a merge, which is the problem the story exists to fix, and there is no new dependency (`sqlite3.exe` 3.53.4 and Python stdlib `sqlite3` are both present). A JSON store and a spreadsheet were weighed and rejected, the spreadsheet because a single binary file is the collision problem again with no line-level merge. Q1b (where the database lives, and rebuilding it from the committed Markdown when it is lost) and Q1c (what a project key is, given worktrees and subdirectories) are new and open; acceptance gained a rebuild check and a concurrency check<br>[2026-09-08] Version 1.2 — the tooling has its own repository, **Discrepo** at `C:\Users\rsant\desar\github\Discrepo`, created with a charter and no implementation; its Phase 0 is this spike. Q1b–Q6 restated there as D2–D7 so the two question lists cannot drift apart, with this story staying where they are answered. Records that ADR numbering is per project — 4thBrain's ADR33 covers only its decision to depend on Discrepo, and Discrepo's own series starts at ADR1 — and that the follow-on implementation story belongs to Discrepo while 4thBrain keeps the adoption work and the `.claude/rules/` rewrites |
| story/P2.1.md | [2026-09-07] OllamaClient and ConcurrencyGate for bounded LLM concurrency (Semaphore(1))<br>[2026-09-08] COMPLETED. Implemented by an agent in its own worktree and merged into `v04` first of three. New `com.fourthbrain.llm` package — `OllamaClient`, `OllamaHttpClient`, `ConcurrencyGate`, `OllamaException` — plus three timeout keys on the `ollama:` block, and 11 tests. Records one re-mapped criterion (injection into `Classifier` belongs to P2.4, and no `Briefing` class exists) and one outstanding gap: the client has never called a real Ollama, because the agent could not start the application |
| design/ADR29-ingestor-boundary.md | [2026-09-08] Created by Story P2.2's design gate. Settles what the Ingestor is for: it does **not** scan `$RAW_DIR` — ADR26 makes `startChain` the only entry point, the actuator is a queue consumer with no timer, and P2.7 owns directory watching — so `raw` becomes the archive destination instead. Sets the routing table, whose load-bearing row sends text to the **Classifier** rather than the Indexer; convertible binary formats stop with a warning naming the format until P2.3 exists, rather than parking at an Extractor that only unzips. Standalone file: three agents wrote ADRs concurrently and could not all append to `ADRS.md`. **Still to be folded into `design/ADRS.md`**<br>[2026-09-08] Decision 4 amended by BUG-005 and the ADR reversed on it: text routes to the `Indexer`, and the `Classifier` runs after it as the terminal stage, so the chain is `Ingestor → Indexer → Classifier`. The heading and the two alternatives arguing for the old order were rewritten rather than left standing, since the file has still not been folded into `ADRS.md` and folding a superseded decision was the worse option. Records why the reversal wins — the vault write is the step whose failure loses a document and should not sit behind a model call — and what it costs, a window in which a published file has no topic or tags |
| design/ADR31-classification-contract.md | [2026-09-08] Created by Story P2.4's design gate, and **clears DD-3**: the Classifier produces tags, manual tagging stays possible and unbuilt. No `classification` table — the result is `document.topic` plus `document_tag` rows, because `schema.sql` is shared and a migration from one of four concurrent branches is the worst place for one. The parser takes the first balanced `{...}` by tracking brace depth, since a lazy regex returns a prefix that is still valid JSON with the tags missing. A parse failure is not a pipeline failure, deliberately the opposite of ADR28's choice for conversions. Standalone file, **still to be folded into `design/ADRS.md`**<br>[2026-09-08] Decision 4 amended by BUG-005 for the reversed chain `Ingestor → Indexer → Classifier`. The Classifier is terminal and returns null on all five paths instead of `"Indexer"`; its guarantee that a failed classification must not strand a document is unchanged but is now a property of the ordering rather than a rule five return statements had to keep. Consequences gained the publication window the reversal opens and lost the claim that the Indexer reads the topic downstream |
| story/P2.2.md | [2026-09-07] Ingestor real logic — read files from RAW_DIR, create Document records, route to TextExtractor<br>[2026-09-08] COMPLETED. Merged second. **ADR29** written as its design gate, striking the story's `$RAW_DIR` scan and routing text to the **Classifier** — a stage registered, threaded and unreachable until now. Captured notes are materialised to disk so they finally reach the vault; the original is archived to `raw`, which nothing had ever written, though `VaultArea.RAW` and `vault.raw` were both defined; PDF and the other convertible formats stop with a warning naming the format rather than being indexed as unparsed bytes. `IngestorRoutingTest` grew from 4 tests to 20. Departure recorded: metadata is persisted through a fresh `Document` carrying only name, extension and mime type, because `updateDocument` would have merged a stale in-memory status over the run loop's write |
| story/P2.3.md | [2026-09-07] TextExtractor real logic — document conversion to Markdown; extraction stack TBD by P2.8 spike<br>[2026-09-07] Rewritten against ADR28. The v03 Node stack (OpenDataLoader, Turndown, Mammoth) is struck; the story now consumes the `MarkdownConverter` seam, archives the original before conversion, and carries P2.9's failure-reason-to-status mapping as a binding contract. Blocker changed from P2.8 (a decision) to P2.9 (a component). Records that no acceptance criterion may assume Markdown headings come back from a PDF. Version 2.0 |
| story/P2.4.md | [2026-09-07] Classifier real logic — LLM tagging via OllamaClient, create Classification and DocumentTag records<br>[2026-09-08] COMPLETED. Merged third. **ADR31** written as its design gate, **clearing DD-3**. No `classification` table: the result lives in `document.topic` plus `document_tag` rows. 33 new tests (24 parsing and normalisation, 9 behaviour). Two things flagged for follow-up: `OllamaClient` is injected `required = false` as a merge-order workaround that should now be tightened to a required injection, and classification has never run against a real model — every test uses a hand-written fake |
| story/P2.5.md | [2026-09-07] Indexer real logic — vault storage and Smart Connections MCP indexing |
| story/P2.6.md | [2026-09-07] Briefing real logic — scheduled synthesis of recent documents via OllamaClient |
| story/P2.7.md | [2026-09-07] File watcher — monitor RAW_DIR for automatic ingestion via WatchService or polling |
| story/P2.8.md | [2026-09-07] Spike: MarkItDown vs Apache Tika as document converter; six integration options analysis; results in ADR and P2.3 rewrite<br>[2026-09-07] Status section added. Three of five acceptance criteria met — analysis, ADR28, P2.3 rewritten — and two not: no fixture corpus and no measurements of any kind. Stays WIP rather than being closed on a decision taken without them. Records the format census and the scanned-PDF split as the two commands to run next, either of which can reverse the stack |
| story/P2.9.md | [2026-09-07] MarkdownConverter seam and its MarkItDown subprocess implementation, carrying out ADR28: the three-method interface, the `com.fourthbrain.convert` package, the full conversion path including staging, stream redirection and descendant-killing on timeout, a startup probe that converts a real non-ASCII fixture rather than calling `--version`, the failure-reason-to-status mapping, 18 acceptance criteria, and eight design placeholders still waiting on P2.8's measurements |
| story/P2.10.md | [2026-09-08] Row added — the file has existed since 2026-09-07 but was never indexed. Environment story standing up Ollama on Windows: native install over WSL2, an explicitly pinned model tag, `OLLAMA_MODELS` set before the first pull, autostart, keep-alive, and a runbook at `design/OLLAMA-SETUP.md`. Verifies `/v1/chat/completions` specifically, because the app never calls `/api/generate` and a green `/api/tags` proves less than it looks like it does<br>[2026-09-08] Version 1.1 — cross-referenced to `design/SPIKE-LOCAL-MODEL-SELECTION.md`, which answers the model-choice question this story defers. Decision 2 now records that the spike's recommendation is *not* `mistral` and what to pin under either outcome; decision 3 gains the 4B size figure; decision 5 gains `OLLAMA_NUM_PARALLEL=1` alongside the keep-alive; step 8 asks for memory *speed*, since DDR4 versus DDR5 selects which column of the spike's throughput table applies. Out of Scope keeps model quality out but now names where it is argued, and records the gap the spike cannot close — no model output has ever been compared against a human answer, P2.4 having closed with every test using a hand-written fake, and no story owns measuring it. `response_format: json_schema` and a capped `num_ctx` explicitly held out as production code<br>[2026-09-08] Version 1.2 — the Out of Scope bullet that recorded "a story that measures tag quality does not exist yet" now names [[P2.11]], which does. Decision 2 notes P2.11 may change the pin again on evidence; decision 3 warns that P2.11 pulls three models, so the storage estimate is for a set rather than one; `blocks` gains P2.11<br>[2026-09-08] Version 1.3 — **COMPLETED**, resolving the contradiction where `BACKLOG-TRACKER` listed it complete and this file's header said READY. Status section added carrying the verification (Ollama **0.33.3**, service answering on 11434, `devstral:24b` and `gemma4:12b` installed) and marking each acceptance criterion met or not: two of six. The one that matters is a defect rather than a documentation gap — `application.yaml` names `mistral`, which is installed nowhere, so the first real Classifier call gets a model-not-found, invisible until now because P2.4's tests all use a fake client and P2.1's has never called a live Ollama. Three criteria carried: no runbook, no `/v1/chat/completions` timing, no reboot check. Each of the five decisions now records what was actually settled, and decision 2 (the model pin) is explicitly handed to [[P2.11]] |
| story/P2.11.md | [2026-09-08] Created. Measures what nothing in the project has ever measured — whether the Classifier's tags are *right*. P2.4 closed with 33 tests and every one of them runs against a hand-written fake, so they prove parsing and normalisation and say nothing about output quality; `application.yaml` names `mistral` because someone wrote it down first, never because it won anything. Exists because `design/SPIKE-LOCAL-MODEL-SELECTION.md` argues for replacing that model on hardware grounds alone, and if the smaller model tags worse nothing currently on file would reveal it. Reserves **ADR34**. Five decisions, two load-bearing: what counts as a correct tag (hand-labelling recommended over inter-model agreement, which measures consensus — three models sharing a bias agree and are all wrong together — and over retrieval, which needs search that is not built), and whether `response_format: json_schema` is a variable or a constant, which decides whether the experiment is a grid or a list. Also asks where the harness lives, since a `@Disabled` JUnit class reads better and drags a live Ollama into a tree `gradle build` runs on every branch. Gives ADR31's accepted "tag vocabulary drifts" consequence a number, and re-uses P2.8's never-run format census so the fixture set matches real inflow. Holds the prompt fixed, because changing prompt and model together makes both results unreadable<br>[2026-09-08] Version 1.1 — **READY**, unblocked by P2.10's completion, and larger for it. P2.10 closed without pinning a model, so this story no longer only chooses between candidates: it produces the first tag `application.yaml` can actually resolve. The candidate set changed with it — no incumbent arm, since pulling `mistral` to defend a choice nobody made is wasted time, and `gemma4:12b` joins as the upper bound because it is already on disk. `devstral:24b` is noted and excluded, being a coding model. Acceptance now requires the chosen tag to appear in `ollama list`, which is the check P2.10 did not make |
| design/ADR35-runtime-model-resolution.md | [2026-09-08] Created as Story P2.12's design gate and carried out in the same pass. The model is whichever one Ollama has loaded, read from `/api/ps` before each call; `ollama.model` is removed rather than corrected, so the configuration can no longer name something that does not exist. Five decisions. Per-call rather than cached, because a cached answer makes a mid-session `ollama run` swap invisible and attributes a result to a model that did not produce it — the one thing P2.11's measurements must not do. The native root is derived by stripping `/v1` instead of gaining a second key, since two keys that must agree will stop agreeing. Nothing loaded throws before `/chat/completions` is contacted, rejecting the implicit-load alternative that produced the original defect and that would surface on this hardware as a 120-second read timeout rather than a diagnosis. Two loaded uses the first and warns, rejecting an ambiguity error as too brittle for the comparison workflow. `isAvailable()` re-defined as "a model is loaded", replacing a probe that returned true in exactly the failing case. Records that **ADR31 is deliberately untouched** — a failed classification stays non-fatal, so nothing loaded means untagged documents rather than a stopped pipeline — and that nothing auto-loads any more, so a reboot leaves every LLM call failing with no startup warning. Standalone file, **still to be folded into `design/ADRS.md`** alongside ADR29, ADR30 and ADR31 |
| story/P2.12.md | [2026-09-08] Created and **COMPLETED** in the same pass, carrying out ADR35. Closes the defect P2.10 surfaced — `application.yaml` naming `mistral` against a machine that has `devstral:24b` and `gemma4:12b` — by removing the configured model name entirely rather than fixing its value. Three files changed: `OllamaHttpClient.java`, `application.yaml` (the key deleted, with the reason left as a comment where it stood) and the test class. `gradle build` green at 106 tests, 0 failures, 2 skipped (`PipelineFlowTest`, unrelated, disabled against BUG-005); `OllamaHttpClientTest` 6 → 12, covering the nothing-loaded path proving no request reaches `/chat/completions`, the two-loaded path, the `name`/`model` field fallback, and the derived native root. The `/api/ps` response shape was checked against the running server rather than assumed. **No live round trip** — every test is against `MockRestServiceServer`, the same gap P2.1 closed with, and exercising it needs a model that fits the machine, which is P2.11's job. Holds out of scope a startup probe (what the application should *do* about nothing being loaded is its own design question) and any change to ADR31's failure semantics |
| story/P3.1.md | [2026-09-07] Unit tests for actuators (JUnit 5, Mockito, >80% coverage of process() logic) |
| story/P3.2.md | [2026-09-07] Integration tests for full message flows through actuator chain (multiple stages, concurrency) |
| story/P3.3.md | [2026-09-07] REST API tests via MockMvc for all endpoints (file/text/URL ingestion, status, search, chat) |
| story/P3.4.md | [2026-09-07] Manual smoke test — browser UI, file upload, watch pipeline progress through dashboard |
| story/P3.5.md | [2026-09-07] Upgrade to JUnit 5 (Jupiter) — migrate all tests from JUnit 4, replace annotations and lifecycle<br>[2026-09-07] Rewritten against what the tree actually contained: the migration had already happened, and the story's real subject is a dangling `junit:junit` on the test classpath with no vintage engine, which turns a JUnit 4 test into a silent zero-test pass. Status section added recording the removal, the `@MockBean` → `@MockitoBean` change folded in, and the four verifications. Status COMPLETED |
| bug/BUG-001.md | [2026-09-07] Web UI fails to load on localhost:8080 or localhost:8080/chat; UIController routing or static resources issue; blocks P1.13 and P3.4<br>[2026-09-07] Diagnosed against the code: UIController and AdminController are `@Controller`s returning view names, and build.gradle carries no template engine, so the names resolve to nothing; `@GetMapping("/")` also suppresses the welcome page that would otherwise serve static/index.html. Missing CSS/JS and static-resource config ruled out. Three fix options recorded, none chosen<br>[2026-09-07] Fix decided: Thymeleaf (ADR25), carried out by Story P1.14. Recorded that the cheaper option was the better one on the evidence and was passed over for roadmap reasons, so the tradeoff survives review. Status READY<br>[2026-09-07] Closed by P1.14 and verified against a running application |
| bug/BUG-002.md | [2026-09-07] A fresh clone will not start: `application.yaml` points SQLite at `data/fourthbrain.db`, SQLite creates the file but not its parent directory, and `data/` is untracked. Found during P1.14 verification, where it masked P1.9's true status. Three fix options; `vault/` and `tmp/` have the same gap |
| bug/BUG-003.md | [2026-09-08] Created. Below 768px the left nav disappears and nothing brings it back, so Search, Ingest status and Admin become unreachable. Two independent causes, either fatal on its own: the hamburger carries an inline `display:none` that outranks the media query meant to reveal it, and no JavaScript has ever referenced `menu-btn` or toggled `.sidebar.open`, so that rule has never had a way to match. Predates P1.18 — P1.7 skeleton markup that P1.18 kept byte-identical — and P1.18 reduced the impact rather than causing it, since the composer is now the panel active on load. Three fix options; A removes the inline style rather than out-specifying it<br>[2026-09-08] COMPLETED via option A. The eight declarations moved from the inline `style` attribute into a `.menu-btn` rule in the `<style>` block, so the `max-width: 768px` rule revealing the button is no longer outranked, and `wireSidebarToggle`/`setSidebar` toggle `.sidebar.open` and close it again when a nav item is chosen. Records how far each criterion was verified: the two structural ones by reading the file, the four describing browser behaviour on the code being right, since nothing here renders a page |
| bug/BUG-004.md | [2026-09-08] Created and closed in the same pass. An upload declared `application/octet-stream` lost its extension and stopped at `ingested`, never reaching the vault. Three chained defects in `IngestionController.processUploadedFile`: octet-stream is not blank so the `isBlank` guard accepted it and the `probeContentType` fallback was dead code for the case it was written for; the extension was derived from the MIME type rather than the filename, so it was discarded; and `Ingestor` checks mime then extension, matched neither, and parked the document. Fixed by taking the extension from `getOriginalFilename()` and resolving the type in a stated order, with parameter stripping and an extension-to-type table covering ADR28's converter formats and the archives routed to the `Extractor`. Evidence was in the working database and in 23 extensionless files in `tmp/`. Records two things held out of scope — the table duplicates `Extractor.guessMimeType`, which P2.3 rewrites, and `newName` computes uniqueness before the extension is appended, which `plan-P2.2.md`'s `VaultNaming` should settle |
| bug/BUG-005.md | [2026-09-08] Created. The full-path chain runs Classifier before Indexer: the required order is `Ingestor → Indexer → Classifier` and the merged code does `Ingestor → Classifier → Indexer`. Not a defect in any one class — each actuator matches its own design, and the disagreement lives in **ADR29** (which changed the Ingestor's text route) and **ADR31**. Records that no test covered the chain before `PipelineFlowTest`, because `IngestorRoutingTest`, `ClassifierTest` and `IndexerTest` each assert one actuator's decision in isolation and all three pass while the chain they compose is wrong. Names four things the reversal has to decide rather than just change, chiefly that a file would reach the vault before it carries a topic, which matters to whatever watches the vault under ADR30<br>[2026-09-08] COMPLETED. All six fix steps taken plus the decision under point 2: the chain is `Ingestor → Indexer → Classifier`, ADR29 and ADR31 amended, `PipelineFlowTest` enabled, the three actuator tests updated, and `Indexer` stopped logging `topic=` now that it is structurally null. 100 tests, 0 skipped. The publication window point 1 names is left open and recorded as belonging to whichever story wires an external indexer |
| design/ADR30-java-mcp-transport.md | [2026-09-08] Created by Story P2.5's Part B, status **Proposed, not decided** — the plan's experiment could not be run without starting the application. Reading the installed server settled three options anyway: `@yejianye/smart-connections-mcp` speaks stdio only with no listener, and its four tools (`lookup`, `connection`, `stats`, `validate`) are all read-side, so nothing indexes, embeds or writes. That eliminates an HTTP endpoint and makes a hand-rolled or SDK MCP client a transport to a server with no call on the far end, leaving option A — the vault write is the trigger — as the only mechanism the installed stack offers. Records the experiment that would confirm it, and that the vault path mismatch would make it fail today for an unrelated reason. **Still to be folded into `design/ADRS.md`** |
| design/SPIKE-LOCAL-MODEL-SELECTION.md | [2026-09-08] Created. Answers the model-choice question Story P2.10 defers, for one machine: Windows, 32 GB RAM, no GPU. Finding is that parameter count outranks bit depth — with no VRAM the limit is memory bandwidth rather than capacity, so 32 GB would hold a 32B model that runs at one to two tokens per second. Q4_K_M for anything at 7B or above; the current `mistral` tag is the older Q4_0, so the quantisation change is free. Recommends a 4B-class model, which is *smaller* than what the project names today, because Mistral 7B dates from 2023 and a 2025-era 4B beats it on instruction-following and structured output at half the size. Records that the Classifier's cost is prefill and not generation, since ADR31's output is ~40 tokens against a whole document of input. Two settings called out as mattering more than the model: `response_format: json_schema` on the OpenAI-compatible surface `OllamaHttpClient` already calls, which constrains decoding so ADR31's balanced-brace parser stops firing, and a capped `num_ctx`. Embeddings weighed as the alternative and rejected on ADR31 — an embedding model can only select from a vocabulary, and ADR31 decided the Classifier produces one<br>[2026-09-08] Version 1.1 — opens by stating that nothing in it is a measurement, and hands the "What to measure" section to Story **P2.11**, which now owns those four numbers and puts them in ADR34 rather than in a later version of this file. Records which of the four can reverse the recommendation (tag agreement, item 4) and why it is the expensive one. The embeddings alternative gains the link that would tell anyone whether its moment has arrived — P2.11 measures the vocabulary drift ADR31 accepted without a number. Closes with the intended order: pin something so behaviour stops drifting, measure, re-pin once |
| simple-claude-plan.md | [2026-09-07] Audit of the Claude governance layer — the nine rules, eight project skills, the solving-loop agent, settings.local.json and the tracking documents. Ten findings, eight referenced files that do not exist, and a three-pass plan cutting 9 rules to 5, 8 project skills to 0, four status-tracking places to 1 and 77 permission entries to about 8 |
>>>>>>> theirs
