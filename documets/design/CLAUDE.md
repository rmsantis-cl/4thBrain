# CLAUDE.md

Before doing anything else in this directory, read `../PROJECT-SUMMARY.md` (`documets/PROJECT-SUMMARY.md`) — it's the current-state summary of the whole project (phase status, story completion, epics, key docs, open gaps). Use it instead of rescanning this directory's files from scratch.

This directory (`documets/design/`) holds the baseline design artifacts themselves (SRS, Epics/Stories doc, ADRs, schema, Gantt chart) — see the summary for which file covers what.
