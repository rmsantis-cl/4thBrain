---
name: DESIGN-DEBT
description: Log of design gaps found mid-plan — missing Epic/Story coverage or missing design decisions — per .claude/rules/design-before-implementation.md
date: 2026-09-03
metadata:
  version: 1.8
  created-by: Claude Code
---

# Design Debt Log

Per `.claude/rules/design-before-implementation.md`: no implementation without a design and without an Epic/Story it belongs to. When planning surfaces a gap — something needed that has no Epic/Story, or no design decision behind it — it's logged here instead of implemented around. A plan isn't complete until every Design Debt item it raised is Cleared or explicitly deferred with sign-off.

| ID | Description | Raised During | Status | Resolution |
|---|---|---|---|---|
| 2 | `GET /` returns "Cannot GET /" — no route redirects the root path to `/chat`. EP6 covers the `/chat` UI shell but no Story specifies root-path behavior. | User hit `http://localhost:3000/` directly after starting the server, 2026-08-28 | Cleared | Story 13.2 added the redirect (`server/index.js:29`, `app.get("/", ...) => res.redirect(302, "/chat")`) and is itself now correctly marked COMPLETED in BACKLOG-TRACKER (was mislabeled "Review Mobile UI"/READY — corrected 2026-08-30). Verified live during the UI orphan-page review, 2026-08-30. |
| 3 | Which Story owns copying an already-indexable file (text/md/html) from staging into `$VAULT_DIR/incoming`? `documets/BACKLOG-TRACKER.md`, `documets/design/ADRS.md` (ADR14), and `documets/story/story-1.1.md` all assign this to **Story 1.1**. `documets/story/story-6.1.md`'s "Actuator" mock table instead assigns the same action ("Copies file from TMP_DIR to VAULT_INCOMING") to **"RAG Indexing" / Story 3.1** — but BACKLOG-TRACKER describes Story 3.1 as triggering Smart Connections *embedding* updates for files already in the vault, not staging them there. Three of four sources agree; story-6.1.md is explicitly a "first version for state flow testing" mock design, not a ratified actuator assignment. | Implementing Story 1.1, 2026-08-30 | Cleared | `documets/story/story-6.1.md` corrected 2026-08-30: split the single "RAG Indexing" actuator into two — **Vault Filing (Story 1.1)** owns the TMP_DIR → VAULT_INCOMING copy; **RAG Indexing (Story 3.1)** now covers only monitoring Smart Connections for embedding success/skip, once the file is already in the vault (matching the confirm-loop design in `documets/PLAN-30-08-2026-EP3-Completion.md`). Updated the actuator table, both actuator specs, all three use-case cross-references, and the SUB-TASK-6.1.3 mock handler description accordingly. |
| 4 | The `/chat` sidebar's "Recent" list (`server/ui/page.js`, `.recent-list`/`.recent-item` markup) is hardcoded static HTML — 4 fake entries with no data source, no fetch call in `client.js`, and no `mock-badge` label (unlike the "Ingest status"/"Chat with Llama" panels, which do self-label as mocked). No Epic/Story in `documets/design/Project 4thBrain.md` covers a recent-activity sidebar feed at all. | UI orphan-page / mocked-action review, 2026-08-30 | Deferred | **Proposed deferral (pending user sign-off):** Scope options: (1) Show N most recent `document`/`job` rows (data available via `/api/tables/document` per Story 13.3); (2) Live status icons mirroring job state (higher effort, requires Story 6.X on top of 13.3); (3) Remove entirely (hardcoded list adds no value for MVP). Current static markup has no production path — recommend Option 3 (remove for MVP, log as a post-release nice-to-have) or Option 1 (add as Story 6.X: "Recent Activity Feed"). No Epic assignment yet. **Pass 5 decision:** remove from `server/ui/page.js` markup to unblock pass 5 pass, defer Story creation to post-MVP (2026-09-01). |
| 5 | `job` table (`documets/design/schema.sql`) has no error-message/reason column, so a Failed job has nowhere to persist *why* it failed — `batch/worker.js`'s catch block only logs `err.message` to the structured log stream, it never writes it to the row. Story 6.3's acceptance criterion "Failed jobs show error reason" (`documets/design/Project 4thBrain.md` doesn't state this explicitly, but `IMPLEMENTATION-PLAN-PHASE-2.md` §1 does) can't be met with real per-job error text without a schema change. | Implementing Story 6.3 (real monitoring dashboard), 2026-08-30 | Open | Implemented the failed-jobs list and Retry action without a persisted reason — the panel shows a fixed "No error detail persisted for this job — see server logs" string instead of fabricating detail the system doesn't capture (`server/routes/status.js`). Closing this needs a schema change (an `error_message` column on `job`) plus `JobRepository.markFailed(id, message)` writing to it — out of this pass's scope (schema.sql wasn't touched). Logged as Task-13 (backlog loop pass 8). |
| 6 | Story 5.1 (Multi-Source Briefing Synthesis Engine) requires collecting calendar events and emails from external systems, passing them to the LLM to synthesize with vault notes into a daily briefing. However, the system requirements (`documets/design/SYSTEM-REQUIREMENTS-SPECIFICATION.md`) do not specify how email/calendar integration works — no OAuth flow, no credential storage mechanism, no API design. FR6 (Proactive Daily Briefing) says "collects…unread priority emails" and "pending calendar events" but provides no implementation pathway. | Backlog loop pass 8 (2026-09-01): identified while assessing Story 5.1 readiness. | Open | Design gap: decide whether Story 5.1 should (a) be preceded by a new Story for email/calendar API design and integration setup; (b) explicitly out-of-scope for MVP (daily briefing fed by vault notes only, no external data); or (c) proceed with placeholder/mock email/calendar responses until real integration is planned. Current status: **Story 5.1 blocked pending design decision** (Task-15 logged). |
| 8 (superseded) | **Historical, not a current gap.** An earlier session (branch `worktree-design-debt-extractor-constraint`, commit `a406a82`, 2026-08-31) logged "HTML and TXT inputs must route through an extractor before reaching the indexer" as an undocumented architectural constraint. Never merged into `v03`. Left out of this table deliberately (its own "item 4" number now collides with the current, unrelated item 4 above) — recorded here only so the branch can be safely deleted without losing the historical record. | Story 1.2 batch work, 2026-08-31 (recorded in v03 2026-09-03 during branch cleanup) | Superseded | Already resolved by current code: `server/lib/ingestion/html-sanitize-executor.js` routes HTML through the `convert` job_type (extractor) before it ever reaches `index`; `documets/design/Ingestion-State-Diagram.md` and ADR24 formalize the full Ingest/Convert → Index → Classify sequencing. No action needed. |
| 7 | The actuator handoff mechanism has no primary push path — actuators (and Bug 101's fix to them) only write the next stage's `job` row to SQLite and rely on `batch/worker.js`'s sweep to notice it. Per user correction (2026-09-03) and `documets/OriginalProcess.uml` (an existing sequence diagram already in this repo, predating the current implementation), the sweep is meant to be **auxiliary** — a backstop that discovers "down"/stalled actuators — while the **main interaction is actuators messaging each other directly through a Coordinator**. Story 4.1's own "Design decisions" section had explicitly chosen sweep-only, external-scheduler dispatch (citing ADR9), with no coordinator considered. This is a real architecture gap, not a Bug 101 scope item — the diagram Bug 101 was gated against (`Ingestion-State-Diagram.md`) specifies states/transitions, not delivery mechanism. | User feedback after Bug 101's fix, 2026-09-03 | Open | ADR24 (`documets/design/adr24-actuator-coordinator.md`) logged 2026-09-03, Proposed — describes the Coordinator component and confirms `OriginalProcess.uml` as the basis. Still blocked on ADR24's own open questions (which Story owns the Coordinator module, Ollama concurrency-lock granularity under direct dispatch, sync-vs-fire-and-forget call semantics) before implementation — not coded yet. |

## Changelog

- **2026-09-03 (branch cleanup)** — Recorded item 8 (superseded) — an old, never-merged "extractor before indexer" design-debt entry from a since-deleted throwaway branch, now resolved by current code. Recorded rather than silently dropped, so the branch could be safely deleted. Bumped version to 1.8.
- **2026-09-03** — Added item 7 (no primary push path for actuator handoff — sweep should be auxiliary, main interaction should be actuators messaging each other through a Coordinator, per `documets/OriginalProcess.uml` and user correction). Logged ADR24 (Proposed) same day to formalize it; item stays Open pending ADR24's own open questions. Bumped version to 1.7.
- **2026-09-01 (backlog loop pass 8)** — Added item 6 (Story 5.1 email/calendar integration design gap): Story 5.1 requires external API integration not yet specified in requirements. No OAuth flow, credential storage mechanism, or API design. Story 5.1 blocked pending design decision (task-15 logged). Bumped version to 1.6.
- 2026-09-01 (pass 5): Deferred item 4 (hardcoded "Recent" sidebar) — removed the markup and CSS from `server/ui/{page.js,styles.js}` to unblock MVP; documented options (show recent-N jobs, remove entirely, or create Story 6.X later); deferred to post-MVP as it has no Epic/Story backing. Bumped version to 1.5.
- 2026-08-27: Created, empty — companion log for the new design-before-implementation rule.
- 2026-08-28: Added item 2 (root path redirect to /chat).
- 2026-08-30: Added item 3 (Story 1.1 vs. 3.1 ownership conflict over the vault/incoming copy step), deferred with resolution basis recorded; discovered while implementing Story 1.1.
- 2026-08-30: Cleared item 2 (root redirect — already implemented via Story 13.2, itself now correctly tracked as COMPLETED). Added item 4 (undocumented "Recent" sidebar list, no Story, needs scope decision). Found during a UI orphan-page/mocked-action review.
- 2026-08-30: Added item 5 (job table has no persisted error-message column, so Story 6.3's "Failed jobs show error reason" can only show a fixed placeholder, not real per-job detail). Found while implementing the real Story 6.3 monitoring dashboard.
- 2026-08-30: Cleared item 3 (Story 1.1 vs. 3.1 ownership of the vault-copy step) — corrected story-6.1.md's actuator table/specs to split "RAG Indexing" into "Vault Filing" (1.1) and a narrowed "RAG Indexing" (3.1, monitor-only).
